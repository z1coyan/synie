# 简道云账务重放 — 交付报告

2026-08-18。PLAN_ID `8f709925`。对照《[简道云账务重放方案](./简道云账务重放方案.md)》。

栈在 `main`（本轮含 `aaa0952b` 钉 1121 CLI + `2da18825` 生产实数）。账务数据已在生产写库；Dokploy 需再发一版才能把 nail/align CLI 带进运行镜像（push `main` 不自动部署）。

## 验收结论

| 项 | 结果 |
|---|---|
| 中国日历 `jdyChinaDate` | **通过**。`2019-12-31T16:00:00.000Z` → `2020-01-01`；naive ISO 钉死 UTC，三时区测试一致 |
| 发票补过账 | **通过**。7 个 PG 用例；无对账单 AUDITED 可补、幂等、拒绝 DRAFT/有对账单、不写 1124 冲回、不调 `toInput` |
| 既有发票审核 | **通过**。8 个 PG 用例（含对账单冲回组）未回归 |
| 承兑补过账 | **通过**。5 个 PG 用例；接收/贴现/调拨拒绝/幂等；`replayBill` 未调用 |
| W5 CLI `delivery-remain` | **通过**（本地 `synie_test` 需先跑到迁移 `00015`）。7 个 PG 用例 |
| CLI 入参 | **通过**。逐 id 失败继续，JSON 收集 error，`--apply` 才写 |
| 截日公式 vs 仪表盘 | **通过**。`formula_asof.py --as-of 2026-08-17`：京泰 58 = **489,921.69**、京泰 95 = **1,238,910.07**、京泰 84 = **285,618.40** / 东方 84 = **349,794.00** |
| 彩排探针 58 / 95 / 84 | **通过**（`synie_replay_check`）。上列四数与仪表盘一致 |
| 彩排主集 `verify_today` | **通过**（2026-08-19 父代理独立复跑）。仪表盘户 \|diff\|>0.01 为 **0 行**。角色口径 − 1122 = 0。京泰/东方 Σ借=Σ贷 |
| 彩排截日 `verify_asof` | **2023-12-31 / 2024-12-31 / 2025-12-31 通过**（0 行）。**2026-08-17 vs 公式** 余 30 京泰 −11,742.40（截止后补收 `A(B)-20260812-0001`，仪表盘有、冻结 `bills.json` 无） |
| 彩排 1121 vs 持有 | **钉死后通过**（`synie_replay_check`，`w4_1121_nail.ts --apply`）。京泰 **1,225,124.92** = 持有 1,227,342.56 − 白名单贷 2,217.64；东方 **0** = 持有 0。白名单外 journal 1121 = **0**。只留 `A(J)-20260514-0030`。持有 31 / 1,227,342.56 未变；客户 1122 7165 行 / 16,521,664.71 未变；Σ借=Σ贷；`verify_today` 主集仍 0 行 |

## 彩排做了什么

本地库 `synie_replay_check` 从 W0 dump `prod_pre_replay_20260818T141716Z.dump` 恢复后：

1. W1 改中国日历：发票 6155 / 其他收付款 43 / 承兑 1981 / 发货 22218
2. W3 发票补过账 6155（缺号的填 `JDY-{doc_no}`，净额=价税、税=0）
3. W4 承兑补过账 1320 + `0001N` + 无入金兑付改 3104
4. W5 发货剩余重过并删 `0008`
5. W6 删找平 `0004`–`0007`（**彩排已删**；生产禁止在主集未绿时删）
6. **W4 对齐**：非 K16 客户 ENDORSE 改 3104；供应商接收改挂客户 1122；误贷撤销；拆段 / 1123→1122 / 未落地客户支出补有日期日记账
7. **主集收口**（2026-08-19）：承兑主因户、YHDZ 错挂/漏挂、混合户 13 行。入口 `w4_align_customer_bills.ts`（含 `receive_untarget`）、`w4j_journals.ts`、`w4m_mixed.ts`
8. **钉 1121**（2026-08-19）：`w4_1121_nail.ts --apply`。YHDZ 1121→3104、cancel R21/R21B、补记改挂、洗分录 75,720。见 P0

## 58 / 95 当初差几百万的原因

发票分项本来就对（58 开票 8,786,057.51）。差额全部在承兑：

- 简道云客户收入有票、synie 记成**供应商接收贷 2202**（不过账台账）。W4 按「无分录就补过」把历史 **exit** 也按客户 ENDORSE 借了 1122，等于把收入抵掉，再漏记未改挂的接收。
- 58：误借转出 2,955,241.40 + 未改挂收入 3,402,359.97 = 6,357,601.37，与 6,847,523.06 − 489,921.69 一致。
- 95 另有两笔客户回款挂在 **1123**（20,000 + 3,600），公式算进 1122。

对齐后四探针与公式/仪表盘一致。

## 生产窗口（2026-08-19 已写库）

回滚点：`.scratch/replay/prod_pre_replay_20260819T110129Z.dump`（另有 08-18 dump）。连接走 `.scratch/prod-cli.sh`（`100.82.52.74:26002` / `synie`）。

| 窗 | 状态 | 说明 |
|---|---|---|
| W0 dump | 已做 | `prod_pre_replay_20260819T110129Z.dump` |
| W1 | 已做 | 发票 6155 / 其他收付款 43 / 承兑 1981 / 发货 22218 |
| W3 | 已做 | 发票补过账 6155，failed=0；live 发票 GL 17031 |
| W4 承兑补过 | 已做 | 0001N 两张、23 兑付改 3104、bill 1320 failed=0；live 承兑 GL 4343 |
| W4 对齐 | 已做 | 计划 `w4_align_plan_prod.json`：ENDORSE 改挂 429、接收改挂 328、拆段/1123/支出 52（其中 2 张因凭证号撞号 skip）。随后 bills_agent / mixed / w4j 29 / w4m 17 / 接收日期 126 / YHDZ cancel 50+relink 50 / R21B 改挂 387 |
| 撞号补记 | 已做 | `w4_align_remainder_prod.json` 5 张：W4S0002 110京泰 34458.72、W4S0007 56京泰 2224.06、W4E0001/0002/0003（与彩排同号） |
| 闸（W6 前） | **通过** | 1122 去掉 0004–0007 vs 仪表盘主集 \|diff\|>0.01 = **0 行**。探针 58/95/84 与仪表盘一致。Σ借=Σ贷 |
| W6 | 已做 | 物理删除 `0004`–`0007`。保留 `0001`/`0001N`/`0002`/`0003` |
| W5 | 已做 | 首轮 4637 结束闸失败（115 张有剩余未对账但不在原 ids）。合并为 4752 后 `delivery-remain --apply`：cancel 4637、重过 366、删 `0008`，failed=0 |
| W8 今天 | **通过** | `verify_today` 0 行；角色口径 − 1122 = 0；`verify_no_plug` 0004–0008 三表+审计 0；`verify_1124` 0 行。1124 京泰 **2,227,366.96**（东方 0） |
| W8 截日 | **通过** | 2023-12-31 / 2024-12-31 / 2025-12-31 主集 0 行。2026-08-17 vs 公式余 **30 京泰 −11,742.40**（`A(B)-20260812-0001`，仪表盘有、冻结 bills.json 无；`verify_today` 对该户为 0） |
| 1121 | **已钉**（2026-08-19） | `w4_1121_nail.ts --apply --allow-prod`。京泰 **1,225,124.92** = 持有 1,227,342.56 − 白名单 2,217.64；东方 **0**。白名单外 journal 1121 = 0。只留 `A(J)-20260514-0030`。持有 31 未变。**1121 不进应收应付报表** |

`w4_1121_yhdz.ts` / `w4_1121_nail.ts` 生产需 `--allow-prod`（默认仍只许彩排库）。

回滚：整窗 `prod_pre_replay_20260819T110129Z.dump`；钉 1121 前 `prod_pre_1121_nail_20260819T141659Z.dump`。

## P0 1121 钉持有（方案验收 4 / 10）

**1121 不进 AR/AP 报表，不要拿报表页验收。**

### 生产实数（2026-08-19 钉前只读）

| 公司 | GL 1121 | 持有 | 差 |
|---|---:|---:|---:|
| 京泰 | 692,508.97 | 1,227,342.56（31 张） | −534,833.59 |
| 东方 | −129,013.30 | 0 | −129,013.30 |

构成：

- 承兑交易 1121：京泰 1,151,622.56（持有 1,227,342.56 − 票 `630252103816920251209101186411` 多贷 75,720）；东方 0。
- journal：YHDZ 387 张仍贷 1121 + R21B 372 张借 1121 对冲（`w4_1121_reclass` 不 cancel 原凭证，K13 不过）。15 张原凭证号京泰/东方共用，R21B 全局唯一号只写了一边，留下 698,320.65 未对冲。
- 白名单 `A(J)-20260514-0030` 贷 2,217.64。
- 票据补记 `A(J)-20191203-0001` 借 100,000 / 贷 1122；`A(J)-20191231-0001` 借 11,511.40 / 贷 1122。禁止 `journals.cancel`（会动 1122）。
- W4J23 借 1121 900 / 贷 1122 户 23，配原 YHDZ `A(J)-20201130-0002`。

75,720：该票两张「银行提示付款补接收」`settle_account=1121`，分录 借1121/贷1121 对冲为零，随后兑付又贷 1121。W4M 已贷 1122 户 26，不能再改 1122。

### 做法（已在 `synie_replay_check` `--apply` 绿）

入口 `w4_1121_nail.ts`。幂等。禁止 `replayBill` / void 持有 / `INSERT acc_gl_entry` / 找平。

1. 剩余 YHDZ 1121 贷方改挂 3104（改 `acc_gl_journal_line` + `acc_gl_entry.account_id`，对账仍挂原凭证）。`A(J)-20201130-0002` 改挂 1122 户 23。
2. `journals.cancel` 全部 `A(J)-R21-%` / `A(J)-R21B-%`（无银行对账）。
3. `journals.cancel` W4J23（与第 1 步 1122 对冲，客户 1122 净额不变）。
4. 两张票据补记 1121 借方改挂 3104，保留 贷1122。
5. 两张洗分录接收贷方 1121→3104，并改 `settle_account_id`。

彩排后：`verify_1121` 京泰 1,225,124.92 = 持有 − 2,217.64；东方 0；白名单外 journal 1121 = 0；`verify_today` 0 行；持有 31 未变；客户 1122 未变；Σ借=Σ贷。

生产已写库（点头后，显式 `DATABASE_URL` 经 `.scratch/prod-cli.sh`）：YHDZ→3104 386、→1122 1、cancel R21B 372、cancel W4J 1、补记 2、洗分录 2，error=0。

钉后闸（生产）：

| 闸 | 结果 |
|---|---|
| `verify_1121` | 京泰 1,225,124.92 / 东方 0；白名单外 journal 1121 = **0** |
| `verify_today` 主集 | **0 行** |
| 角色口径 − 1122 | **0** |
| `verify_asof` 2023/2024/2025-12-31 | **0 行** |
| `verify_no_plug` | 0004–0008 三表+审计 **0** |
| 探针 58/95/84 京泰、84 东方 | 489,921.69 / 1,238,910.07 / 285,618.40 / 349,794.00 |
| Σ借=Σ贷 | 京泰、东方都平 |
| 持有 | 31 / 1,227,342.56 未变 |
| 客户 1122 | 7165 行 / 16,521,664.71 未变 |

## P1 例外户（K6，不找平）

账面 = 生产 1122 科目、客户往来、未作废。仪表盘/v3 来自 `.scratch/align/jdy_targets_v3.csv`。禁止用 2020-01-01 找平补。

| 户 | 公司 | 账面 1122 | 仪表盘 | v3/公式 | 原因 | 建议 |
|---|---|---:|---|---|---|---|
| 8038 鲸耀 | 京泰 | 121,760.00 | 无此户 | −17,490,440 | 开出发票 291,760 − YHDZ `A(J)-20241223-0004` 货款 170,000。v3 `manual_carry` 是旧口径 | 保持来源单据自然账面。要对 8038/USD 做业务定性须另开项目 |
| 8038 鲸耀 | 东方 | 4,800.00 | 无此户 | 0 | 1 张开出发票 | 同上 |
| 538 锦州森源 | — | 无档（主档 232 同名户=0） | 无此户 | 4,213.90（`formula_only`） | 编号 538 在 lookup 里有、客户档无 | 不补建、不过该票 GL；known_gaps |
| 8044 / 8046 / 8047 / 8052 / 8053 / 8059 / 8062 / 8065 / 8086 | 京泰（8052 亦有东方 0） | **0** | 无此户 | v2_carry 非 0 | synie 补档；找平 `0004`–`0007` 已删，来源单据轧差为 0 | 接受 0，不要把 v2 目标找平回去 |
| 8081 | 东方 | −8,727.98 | 无此户 | v2_carry 4,018.08 | 发票 4,018.08 − YHDZ 货款四笔 12,746.06 | 自然账面；不找平 |
| 8084 | 东方 | −32,805.22 | 无此户 | v2_carry 36,579.41 | 发票 36,579.41 − YHDZ 过滤网款 69,384.63 | 自然账面；不找平 |

非仪表盘 80xx 开出票仍挂 1122（`verify_today` 故意不把它们当超差；公司合计会大于仪表盘）：

| 户 | 公司 | 账面 1122 | 说明 |
|---|---|---:|---|
| 8006 | 京泰 | 2,663,672.62 | 不在 v3 |
| 8030 | 京泰 | 693,829.66 | 不在 v3 |
| 8037 | 京泰 | 552,795.97 | 不在 v3 |
| 8023 | 京泰 | 125,000.00 | 不在 v3 |
| 80 | 京泰 | 41,521.64 | **在仪表盘**（安徽翔登），主集已对齐 |
| 8020 / 8028 | 京泰 | 30,957.32 / 21,889.40 | 不在 v3 |
| 8076 | 东方 | 34,999.65 | 不在 v3 |
| 8058 | 东方 | 12,600.00 | 不在 v3 |
| 8007 / 8050 / 8048 / 8063 / 8077 | 京泰或东方 | 见库 | 不在 v3 |

## 3104 增减表（方案验收）

生产未作废 3104（钉 1121 前；钉后 R21B 贷方会随 cancel 消失，YHDZ 原 1121 贷方改挂进来）。

| 来源 | 京泰净额 | 东方净额 | 说明 |
|---|---:|---:|---|
| `A(J)-20200101-0001` | −2,291,190.91 | −5,228,225.35 | 真期初对侧（贷） |
| `A(J)-20200101-0001N` | +123,360.00 | +54,614.70 | 7 行负数期初（借）。京泰 122,860+500；东方 36,156+9,371.50+5,565+3,022.20+500 |
| 无入金兑付 settle 借 3104 | +1,573,554.37（19 笔） | +115,031.00（4 笔） | 生产实数 23 笔（方案写 85，窗口改挂 23） |
| 客户 ENDORSE 改挂 3104 | +27,795,963.61（313） | +8,094,666.24（117） | W4 非 K16 |
| W4M | +311,867.29（9） | 0 | 混合户补记 |
| R21B（钉前） | −24,231,033.39（268） | −6,282,051.82（104） | 已 `journals.cancel`，不应再挂 1121/3104 |

彩排钉后 3104 合计：京泰借 38,344,622.51 / 贷 37,735,612.66 净 +609,009.85；东方借 9,595,800.62 / 贷 15,127,020.29 净 −5,531,219.67。不是 1122 找平。

## 日期 vs `jdyChinaDate`；`doc_no` 未因 W1 改号

抽 50 张已导入发票（`remarks` 含 `jdy=`）：`invoice_date` 与 raw `_widget_1743342957248` 的 `jdyChinaDate` **50/50 一致**。

W1 断言：发票 6155 / 6155 的 `doc_no` 日期段 ≠ `invoice_date`（号日早一天，业务日已改中国日历）。若 W1 重写过号，不会 100% 保留这道疤。W1 SQL 同一事务内 `md5(string_agg(doc_no))` 前后相等，否则 `RAISE EXCEPTION 'doc_no hash changed'`。

## 未做（2026-08-19 自主决定仍不做）

与 1122/1124/1121 解耦，不在冻结窗收口里。工行缺口 −34,175、农商疑漏记 +47,864.95 留档，不用 1122 补。

- P2 W7 银行期初拆张 `0002`/`0003`（禁止 `journals.create`）。
- P3 D3 库存实盘期初（应迁 0，仍待实盘）、D4 负库存开关（迁完应关）、D5 超发/超收/超入容差（库内临时 1000，迁完应收回）。

## 怎么用

```
# 默认 dry-run
bun scripts/jdy-replay/backfill_cli.ts --kind invoice --ids-file /tmp/inv.ids
bun scripts/jdy-replay/backfill_cli.ts --kind bill --ids-file /tmp/bill.ids
bun scripts/jdy-replay/backfill_cli.ts --kind delivery-remain --ids-file /tmp/del.ids

python3 scripts/jdy-replay/build_w4_align_plan.py -o .scratch/replay/w4_align_plan.json
bun scripts/jdy-replay/w4_align_customer_bills.ts --plan .scratch/replay/w4_align_plan.json --apply

# 钉 1121。生产必须 --allow-prod，且先 dump、等人点头
bun scripts/jdy-replay/w4_1121_nail.ts
bun scripts/jdy-replay/w4_1121_nail.ts --apply --allow-prod

python3 scripts/jdy-replay/formula_asof.py --as-of 2024-12-31
# 闸：见 scripts/jdy-replay/README.md
```
