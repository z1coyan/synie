# 简道云账务重放 — 代码交付报告

2026-08-18。PLAN_ID `8f709925`。对照《[简道云账务重放方案](./简道云账务重放方案.md)》。

**本轮交付范围：PR1–PR6 代码与校验脚本。生产冻结夜 W0–W8 未执行。**

## 验收结论

| 项 | 结果 |
|---|---|
| 中国日历 `jdyChinaDate` | **通过**。`2019-12-31T16:00:00.000Z` → `2020-01-01`；naive ISO 钉死 UTC，三时区测试一致 |
| 发票补过账 | **通过**。7 个 PG 用例；无对账单 AUDITED 可补、幂等、拒绝 DRAFT/有对账单、不写 1124 冲回、不调 `toInput` |
| 既有发票审核 | **通过**。8 个 PG 用例（含对账单冲回组）未回归 |
| 承兑补过账 | **通过**。5 个 PG 用例；接收/贴现/调拨拒绝/幂等；`replayBill` 未调用 |
| W5 CLI `delivery-remain` | **通过**（本地 `synie_test` 需先跑到迁移 `00015`）。7 个 PG 用例：整单 cancel + 剩余两行 + 同事务删 `0008`；闸失败回滚；空 ids 不删 `0008` |
| CLI 入参 | **通过**。8 个单测：默认 dry-run、`--apply` 才写 |
| 截日公式 vs 仪表盘 | **通过**。`formula_asof.py --as-of 2026-08-17`：京泰 58 = **489,921.69**、京泰 95 = **1,238,910.07**、京泰 84 = **285,618.40** / 东方 84 = **349,794.00**，与 align5 / 生产抽查一致。`--as-of 2020-01-01` 京泰 58 = **107,756.72**（真期初，不是找平后的 199 万） |

本地 `synie_test` 初次跑 W5 失败是因为库停在迁移 00014（`acc_bank_reconciliation.journal_id`）。补跑 `00015_bank_recon_source_voucher` 后 7/7 绿。生产已是 `voucher_type` + `voucher_id`，与代码一致。

## 栈

plain-git，未自动开 GitHub PR。顺序：

| 序 | 分支 | 内容 |
|---|---|---|
| 1 | `execute-plan/8f709925-pr-1-fixjdy` | `jdyChinaDate` |
| 2 | `execute-plan/8f709925-pr-2-featinvoice` | 发票 `backfillPostedGL` |
| 3 | `execute-plan/8f709925-pr-3-featbill` | 承兑 `backfillPostedGL` |
| 4 | `execute-plan/8f709925-pr-6-docs` | 重放方案 + 迁移方案指针 |
| 5 | `execute-plan/8f709925-pr-4-featreplay-backfill-cli-systempermit` | CLI + W5 `delivery-remain` |
| 6 | `execute-plan/8f709925-pr-5-choreplay` | `formula_asof` + `verify_*.sql` |

栈顶：`origin/execute-plan/8f709925-pr-5-choreplay` @ `608fc2ac`。

开 PR（自下而上，`--base` 为栈中下一层）：

```
gh pr create --base main --head execute-plan/8f709925-pr-1-fixjdy --fill --draft
gh pr create --base execute-plan/8f709925-pr-1-fixjdy --head execute-plan/8f709925-pr-2-featinvoice --fill --draft
gh pr create --base execute-plan/8f709925-pr-2-featinvoice --head execute-plan/8f709925-pr-3-featbill --fill --draft
gh pr create --base execute-plan/8f709925-pr-3-featbill --head execute-plan/8f709925-pr-6-docs --fill --draft
gh pr create --base execute-plan/8f709925-pr-6-docs --head execute-plan/8f709925-pr-4-featreplay-backfill-cli-systempermit --fill --draft
gh pr create --base execute-plan/8f709925-pr-4-featreplay-backfill-cli-systempermit --head execute-plan/8f709925-pr-5-choreplay --fill --draft
```

Compare：

- https://github.com/z1coyan/synie/compare/main...execute-plan/8f709925-pr-1-fixjdy?expand=1
- https://github.com/z1coyan/synie/compare/execute-plan/8f709925-pr-1-fixjdy...execute-plan/8f709925-pr-2-featinvoice?expand=1
- https://github.com/z1coyan/synie/compare/execute-plan/8f709925-pr-2-featinvoice...execute-plan/8f709925-pr-3-featbill?expand=1
- https://github.com/z1coyan/synie/compare/execute-plan/8f709925-pr-3-featbill...execute-plan/8f709925-pr-6-docs?expand=1
- https://github.com/z1coyan/synie/compare/execute-plan/8f709925-pr-6-docs...execute-plan/8f709925-pr-4-featreplay-backfill-cli-systempermit?expand=1
- https://github.com/z1coyan/synie/compare/execute-plan/8f709925-pr-4-featreplay-backfill-cli-systempermit...execute-plan/8f709925-pr-5-choreplay?expand=1

## 未做（按方案故意留下）

生产 W0–W8：dump、改日期、6155 张发票过账、承兑重放、删找平 `0004`–`0008`、重写 1124。须等本栈合入并发布后，冻结夜按方案 Rollout 用 `scripts/jdy-replay/backfill_cli.ts` 执行。W3+W4+W6 同一夜，中间态今天 1122 双计，不得交回营业。

## 怎么用（合入后）

```
# 默认 dry-run
bun scripts/jdy-replay/backfill_cli.ts --kind invoice --ids-file /tmp/inv.ids
bun scripts/jdy-replay/backfill_cli.ts --kind bill --ids-file /tmp/bill.ids
bun scripts/jdy-replay/backfill_cli.ts --kind delivery-remain --ids-file /tmp/del.ids

# 真写
bun scripts/jdy-replay/backfill_cli.ts --kind invoice --ids-file /tmp/inv.ids --apply

# 截日公式
python3 scripts/jdy-replay/formula_asof.py --as-of 2024-12-31
# 闸：见 scripts/jdy-replay/README.md
```
