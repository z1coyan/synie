# 简道云业务数据迁移实录（2026-08-12 ~ 08-15）

承接《[简道云业务数据迁移方案](./简道云业务数据迁移方案.md)》与《[简道云数据迁移](./简道云数据迁移.md)》（主数据+销售报价）。本文只记**执行结果与坑**，策略与决策以方案为准。

cutoff：**2026-08-31**。线上：`https://erp.tzjingtai.com`。

## 1. 决策执行记录

| # | 取值 | 执行落地 |
|---|---|---|
| D1 | cutoff=2026-08-31 | 写入 run 元数据与各 state |
| D2 | 已票账两清不迁发票，只迁在途未票链 | **2026-08-13 修订**：发票全量作台账导入（AUDITED、**不过账**，往来仍在期初凭证）；对账仍未导 |
| D3 | 实盘后手工出入库建账 | **未做**（无实盘清单） |
| D4 | 导入期打开相关仓 `allow_negative` | JT/DF 叶子仓已开；迁完应关闭 |
| D5 | 超发/超收临时放宽，迁完收回 | **库内** `delivery_overship_ratio` / `receipt_overreceive_ratio` = **1000**（API 校验 0~1，故 drop CHECK 后 SQL 写入）；迁完必须恢复 0 + 重建 CHECK |
| D6 | 报价占位截止日 2099-12-31 | 沿用 |
| D7 | 23 张冲突报价不导入 | 沿用 |
| D8 | 委外缺档补供应商+外协仓 | 外协仓 14 已建；委外单据链未导 |
| D9 | 承兑两表对账取更全一份 | `bills_canonical` 1312；在手持接收已导 173 |
| D10 | 销售订单/出库全量进业务表（历史不过账） | **已执行**：出库 22218 头齐；见 §5、§10 |

**铁律（执行中纠正）**：

- **禁止**为绕超发而新建「客户未下」的扩容订单（虚假订单）。对账需要的历史出库必须进发货单，不能只改订单已发数量。
- 历史超发：只改**系统容差**（含必要时 SQL 突破 API 0~1 上限），**不**编造订单数量。
- 有过账后果的在途单据走 API；已结清历史台账（发票、补导出发货）SQL 直插 AUDITED、不过账，避免和期初凭证双计。
- 对账要用的销售订单/出库**全量进业务表**（D10），不因「轨道 C 只留 JSON」丢掉可查单据。

## 2. 公司与配置

| 公司 | code | 说明 |
|---|---|---|
| 台州京泰电气有限公司 | JT | 既有 |
| 玉环县东方钢板网厂 | DF | **2026-08-12 新建**（当时误写成「台州东方电气有限公司」）；**2026-08-13 更正**法定名称，简称仍为「东方」；科目模板 SMALL 77、七往来角色、默认过账四槽、种子仓 |

- 银行账户 6：JT 4（00001/02/05/06）+ DF 2（00003/04）
- 客户地址约 317；外协仓 14

## 3. 各模块导入结果（线上实数，2026-08-13）

| 模块 | 简道云/应迁口径 | 已迁（线上） | 留档/未迁 | 备注 |
|---|---:|---:|---|---|
| 银行流水 | 清洗表 6472 + 截止日后 60 | **6532** | 0 | 2026-08-15 按 `~/银行流水.xlsx` 覆盖重导，见 §16 |
| 银行账户 | 6 | **6** | 0 | |
| 期初凭证 | 2（JT+DF） | **2 AUDITED** | 0 | JT 应收≈229 万；DF 应收≈523 万；贷方利润分配 |
| 承兑 | 简道云去重后 1037 票号（在手持 434 + 历史 878 行） | **1037 主档 / 1669 交易 / 持有 433** | 与已在手持重叠的 260 条历史行不重复建段 | 2026-08-13 全量补导：剩余在手持走接收审核；历史票接收+转让/兑付不过账、不占持有 |
| 销售订单 | 简道云 14000 头 | **14320**（约 154 已审核真未发 / 其余已关闭） | 6 头无条目未建 | 全量留档；关闭单仍可查收发货历史 |
| 销售发货 | 简道云 22218 头 / 43623 行 | **22218 / 49757** | 0 头 | 原 4889 走过账；补导 17329 台账不过账；多出行来自分摊到多张订单 |
| 销售报价 | 主数据阶段已迁 + 迁移补挂 | **~3112** | — | 含为挂订单做的报价条目；**禁止再为发货造单** |
| 增值税发票 | 6184 | **6155 AUDITED** | 29 | 台账导入、不过账；跳过 22 重复票号 + 4 自开 + 3 无法判方向 |
| 采购/委外/对账 | 有载荷 | **0** | 全留档或未做 | 采购委外链、对账单未导入 |
| 库存期初 | 0 | 0 | 待实盘 | D3 |

状态机旁注（**2026-08-13 二次修补后**）：

| 状态 | 发货 | 订单 | 发票 |
|---|---:|---:|---:|
| audited | **22218** | **~154**（真未发） | 6155 |
| closed | — | **~14166**（历史已结清，单据仍在） | — |
| voided | **0** | **0** | 0 |
| draft | 0 | — | 0 |

曾误建的 204 张「迁移扩容订单」+ 5738 张挂其上的发货（及 10616 条已作废 GL、11305 条已作废库存分录）已于 2026-08-13 **物理删除**，列表不再出现已作废垃圾单。

## 4. 超发容差（D5）技术说明

| 路径 | 上限 |
|---|---|
| API `PATCH /settings/supply-chain` | **0～1**（100%） |
| 表 `sal_setting` CHECK | 原 `<= 1` |
| **迁移临时（SQL）** | drop CHECK 后写入 **1000**（即 100000%） |

恢复模板（迁完执行）：

```sql
UPDATE sal_setting SET
  delivery_overship_ratio = 0,
  receipt_overreceive_ratio = 0,
  updated_at = (now() AT TIME ZONE 'utc');
ALTER TABLE sal_setting ADD CONSTRAINT delivery_overship_ratio_range
  CHECK (delivery_overship_ratio >= 0 AND delivery_overship_ratio <= 1);
ALTER TABLE sal_setting ADD CONSTRAINT receipt_overreceive_ratio_range
  CHECK (receipt_overreceive_ratio >= 0 AND receipt_overreceive_ratio <= 1);
```

（若业务要保留 100% 过渡，可改为 `= 1` 再加 CHECK。）

## 5. 发货覆盖率说明（2026-08-13 全量补导后）

| 口径 | 数量 |
|---|---:|
| 简道云销售出库头 / 条目 | **22218 / 43623** |
| synie 发货单 / 发货条目 | **22218 / 49757** |
| 其中 API 审核过账（已发未对账） | 4889 单 |
| 其中 SQL 台账不过账（已对账 + 老 Excel 出库） | 17329 单 |

出库挂订单（补导 17329 单内，一行可拆到多张订单故条目多于源）：

| 挂法 | 行数 | 说明 |
|---|---:|---|
| 简道云出库条目 → 订单条目 lookup | 9280 | 最可靠 |
| 同一客户 + 同一物料，按日期 FIFO | 8512 | 老 Excel 出库无 lookup |
| 客户料号 / 名称+规格 | 9877 | lookup 与物料编码都对不上时 |
| 仍无主档：留档订单 + 行快照 | 8633 | 快照保留简道云原编码/名称/规格；单备注「简道云出库未映射物料留档」 |

发货条目 49757 > 源 43623：同一出库行分摊到多张订单时拆行。头数 22218 与简道云一致，对账按出库单号（备注 `简道云出库:…`）查。

## 6. 产物路径

**原始快照（重洗/补库存/补凭证用这个，勿删）**：`data/jdy-source/`（gitignore，约 230MB JSON）。附件正本在阿里云 OSS `synie-erp`，对照表 `data/jdy-source/attachments/jdy-to-oss.jsonl`，不要再从简道云下、不要再传。约定见《[简道云原始快照](./简道云原始快照.md)》。

| 路径 | 内容 |
|---|---|
| `data/jdy-source/raw/` · `master/` | 冻结的简道云原始 JSON |
| `data/jdy-source/attachments/` | JDY→OSS 对照、线上 sys_file/sys_attachment 导出 |
| `.scratch/jdy-migration/raw/` | 同内容工作副本（冻档时硬链） |
| `.scratch/jdy-migration/clean/` | 规则清洗 |
| `.scratch/jdy-migration/ai/` | AI 映射 |
| `.scratch/jdy-migration/payloads/` | 导入载荷与 archive |
| `.scratch/jdy-migration/state/` | 阶段 state、扩容作废清单 |
| `.scratch/jdy-migration/reports/` | 分阶段与验收报告 |
| `.scratch/jdy-migration/attach/` | 本地下载缓存（约 2.3G；OSS 已有正本后可删） |
| `.grok/workflows/jdy-migration.rhai` | 无人值守编排脚本 |

本地鉴权文件（**勿入库**）：`.scratch/jdy-migration/auth.env`。

## 7. 未完成与后续

重洗或补库存/凭证时从《[简道云原始快照](./简道云原始快照.md)》的 `data/jdy-source/` 读原文，不要再全量拉简道云，也不要用本轮 `payloads/` 当事实源。

1. **恢复 D5 容差 + CHECK**（当前 SQL=1000）
2. **关闭 D4 负库存**（盘点后）
3. 采购订单/入库/委外链
4. 销售/采购对账单（发票已作台账导入，未挂对账单、不过账）
5. 承兑已全量补导（见 §9）
6. 库存实盘期初（D3）
7. V1～V6 正式验收
8. 3 个未进主档的新物料未建对应未发订单：`F(P)77-s1017c.01.04`、`F(P)9-135`、`F(P)9-134`（主档建好后补单）
9. 为挂发票新建的客商（客户 +88 / 供应商 +449，编号 8xxx / 3xxx）可日后与主档合并
10. 留档单上无主档出库：2026-08-14 重洗后 `F(HIST)-UNMAPPED` 仍剩约 4900 行（多半是 Excel 代、旧应用无 UUID）；对账靠行快照，见 §11

## 8. 坑（本轮）

1. API 超发比例硬限 0～1，与「临时放很大」冲突 → 必须 **SQL 改库 + drop CHECK**，不能只调 API。
2. **扩容订单 = 虚假数据**，已整批物理删除；后续禁止。
3. 客户物料一料一客：跨客户发货无法仅靠超发解决。
4. 发货 void 后列表 count 含 voided 行；垃圾单应物理删掉，不要留作废痕污染期初。
5. 第二家公司不能凭简称「东方」脑补成「东方电气」——票面法定名称是 **玉环县东方钢板网厂**。
6. 发票若走审核过账会和期初凭证双计应收；期初发票只进台账、不过账。
7. 简道云订单条目 `_widget_1745818019983`=**未发**、`9984`=**已发**；载荷用反后把已发完的单当成在途导入。真未发口径是「需要发货 ∧ 未发>0」（约 200 行，与未发货清单一致）。
8. 对账必须有出库单据：只改 `shipped_qty` / 关闭订单不够；历史出库要进发货单，已结清部分不过账。

## 9. 2026-08-13 二次修补

针对上线后发现的三处遗留：

1. **公司名**：DF 从误建的「台州东方电气有限公司」更正为票面法定名称「玉环县东方钢板网厂」（code=`DF`、简称「东方」不变）。简道云发票销方/购方本来就是这个名字（销 799 / 购 355）。
2. **已作废订单**：204 张全是「迁移扩容订单」（为绕超发而编造），不是真实作废业务；5738 张已作废发货是挂在这些扩容单上后又作废的重导残留。已整批物理删除，订单/发货列表只剩已审核真实单。
3. **发票**：上一轮 D2 把 6184 张全部进了 archive、一张没导。本期 SQL 直插 **6155** 张已审核台账（JT 5018 / DF 1137；开出 4127 / 开入 2028），票面、明细、三科目齐全，**不派生总账**（应收应付仍以两张期初凭证为准，避免和发票分录双计）。对手按名称/编号匹配既有客商，对不上的补建主档。

脚本：`.scratch/jdy-migration/scripts/fix_company_purge_import_invoices.py`。

### 附件补挂（同日稍后）

发票原件与承兑正反面此前只下到 `.scratch/jdy-migration/attach/`，没挂到单据。已直传 OSS 并写入 `sys_file` / `sys_attachment`：

| 宿主 | 附件条数 | 覆盖单据 |
|---|---:|---:|
| 增值税发票 `acc_vat_invoice` / `original` | 4951 | 4879 张（列表「票面」列可预览） |
| 承兑接收 `acc_bill_transaction` | 369 | 120 张已导入接收（正+反+库存承兑图，列表「票面」显示 `+2`） |

承兑图已随全量补导再挂：`acc_bill_transaction` **1840** 张，覆盖 **351** 笔接收。脚本：`.scratch/jdy-migration/scripts/import_attachments.ts` / `import_all_bills.py`。

## 10. 2026-08-13 未发对齐与出库全量补导

业务要求：和客户对历史账必须能查到订单和出库，**所有销售订单/出库都要进 synie，且不能靠改余量冒充已发**。

### 10.1 未发口径（先纠字段）

简道云销售订单条目聚合字段：

| widget | 标签 |
|---|---|
| `_widget_1745818019983` | **未发货数量** |
| `_widget_1745818019984` | **已发货数量** |

上一轮载荷把 9984 当成未发、把「已发>0」当成在途，用已发数量当订购数量导入。结果：已发完的单大量显示未发；真正「需要发货且未发>0」约 200 行大部分没进来。

真未发 = 简道云「需要发货 ∧ 未发>0」，与导出的《未发货清单》**200 行**一致。老 Excel 出库没挂订单，简道云靠去掉「需要发货」关单，未发是虚数。

| 动作 | 数量 |
|---|---:|
| 已导入但不在清单：先把余量清掉并关闭 | 4165 单 / 6475 行 |
| 已导入且在清单：余量收到简道云未发 | 30 行 |
| 清单缺失：按未发数量新建并审核 | **88** 单（含 1 张报价有效期重试） |
| 未建：物料不在 synie 主档 | 3 行（`F(P)77-s1017c.01.04`、`F(P)9-135`、`F(P)9-134`） |

脚本：`.scratch/jdy-migration/scripts/fix_unshipped_align.py`。

### 10.2 出库全量进发货单（D10）

只改 `shipped_qty` 对账看不见出库。同日按 D10 把简道云全部销售出库补成发货单：

| 项 | 数量 |
|---|---:|
| 简道云销售出库头 | 22218 |
| synie 发货单 | **22218**（原 4889 API 过账 + 补 17329 台账不过账） |
| 发货条目 | 49757（一行出库拆到多张订单时会多行） |
| 补齐销售订单 | +9726 张（订购数量用简道云原数量） |
| 出库挂订单 | 见 §5 |

无主档物料的出库行挂「简道云出库未映射物料留档」订单，行快照保留简道云原编码/名称/规格。补导不派生总账/库存（期初凭证与已过账的 4889 张发货已记账）。历史已结清单关闭后仍可打开看出库。

补导后已审核未发约 **171 行 / 25.4 万**（清单未发合计 25.3 万）。脚本：`.scratch/jdy-migration/scripts/import_all_deliveries.py`。

## 11. 2026-08-14 旧应用 UUID 重洗销售链

上一轮只抽了新应用，2025-03 之前出库没有 lookup，只能 FIFO / 名称规格猜，还把一行出库拆到多张订单。旧应用「京泰电气」里订单行、出库行都有自制 `辅助字段-订单条目ID`，按 1 出库行 : 1 订单行改挂（一订单行仍可对应多次出库）。

| 项 | 数量 |
|---|---:|
| 旧应用订单 / 行（UUID 全覆盖） | 9489 / 15499 |
| 旧应用出库 / 行（有 UUID） | 16910 / 33134（19384 有 UUID） |
| 旧应用产品 | 4758 |
| 处理的 `销-*` 发货单（不动 `W(SO)-`） | 17145 |
| UUID 对上订单行 | 12755 |
| 改挂 `order_item_id` | 8598 + 165（两轮） |
| 物料改到 `F(P)…` | 3922 |
| 从 HIST 留档恢复名称/规格快照 | ~4900 |
| 仍挂 `F(HIST)-UNMAPPED` | ~4907（Excel 代无 UUID 为主） |
| 未配对的多余发货行 | ~8686（拆行残留；§12 已处理改挂，未删行） |

脚本：`.scratch/jdy-migration/scripts/fetch_old_app.py`、`rewash_sales.py`。旧应用原文：`data/jdy-source/old-app/`。不过账、不改 C 代 lookup、不碰期初凭证。

核验例：`销-250122001` 的 2000/`F(P)58-87` 改挂 `SO20241223304`，505/`F(P)58-134` 改挂 `SO20250117531`（原先都挂在 2026 年订单上）。§12 后这两行挂接仍在。

未做（§11）：流水重洗、拆行合并、Excel 代补订单、采购/对账。拆行残留见 §12。

## 12. 2026-08-14 收干净销售链拆行残留

承接 §11。FIFO 导入把一行简道云出库拆到多张订单；UUID 改挂只动了能对上的那一片，碎片仍挂在更晚的真订单上（把别的单的已发抬高）。本轮只 SQL 改挂 / 重算 `shipped_qty`：不改订购 `qty`、不过账、不删真实出库事实、不新建扩容单、不动 `W(SO)-` / `XSFH`。

**超发是业务常态**（产品交货本来就会超订购数量），不是迁移错误。本轮要拿掉的是挂错单的拆行碎片；挂对之后 `shipped_qty > qty` 应保留，不要再为「收到 0 条超发」去改挂或扩容。

脚本：`.scratch/jdy-migration/scripts/cleanup_split_fragments.py`（默认 dry-run，`--export` 重拉线上表，`--apply` 先 dump `sal_delivery_item`+`sal_order_item` 再写库）。对照：`.scratch/jdy-migration/rewash/frag_*.csv`、`frag_stats.json`。事实源仍是 `data/jdy-source/old-app/` + `raw/`。

### 算法

对 `remarks` 以 `简道云出库:销-` 开头的发货单：

1. 应有行 = 旧应用出库行（没有旧单则用新应用 `ship_items`）。
2. 实有行 = 线上 `sal_delivery_item`。
3. 主行 = 已按 UUID 挂在目标订单、且物料/图号（或名称规格）对得上应有行的（数量相等优先，拆片覆盖次之）。
4. 对不上的 = 碎片；`reconciled_qty=returned_qty=0` 才能动。
5. 碎片处理：
   - 该应有行有 UUID 目标且尚未盖满 → 补挂回目标订单行（§11 漏网，不是新建订单）。
   - 主行数量已覆盖应有行、且删了发货单 Σqty 仍 ≥ 简道云 Σqty → 删除。
   - 否则改挂该客户「简道云出库未映射物料留档」订单行；没有留档单就跳过。
6. 全量 `shipped_qty = Σ` 仍挂在该行上的发货数量；没有任何发货的置 0。
7. `F(HIST)-UNMAPPED` 仍挂真单的，改挂留档。

### 执行结果（线上，2026-08-14）

| 项 | 数量 |
|---|---:|
| 处理的 `销-*` 发货单 | 16910（跳过 `W(SO)-` 5073 + `XSFH` 235） |
| 应有行 / 实有行 | 33134 / 38910 |
| 主行（exact UUID / 拆片覆盖） | 12444 / 3817 |
| 应删 | **0**（FIFO 保出发货单 Σqty = 简道云 Σqty，没有「主行已满 + 可删重复」） |
| 补挂回 UUID 目标订单行 | 6603 |
| 改挂留档 | 7464（含 112 条仍挂真单的 `F(HIST)-UNMAPPED`） |
| 未动：已在留档 | 8501 |
| 未动：该客户无留档单 | 193（不新建扩容单） |
| 发货头 / 行 | **22218 / 49756**（头不变；行数未降，不强求一次到位） |
| 真订单 `shipped_qty > qty` | 8863 → **5700**（挂对之后的真实超发，属业务常态，不是残留） |
| `F(HIST)-UNMAPPED` 挂真单 | 112 → **0**（4907 行全部在 284 张留档上） |

订购数量：旧应用 9489/9489 与 `old-app/so.json` 仍全部一致，本轮未改 `sal_order_item.qty`。C 代（新应用 `SO-YYMMDD-`、不在旧应用）抽查 4505 张，4496 合计一致；9 张线上订购合计小于新应用 `so_items`（`SO-260808-0004` 等，上一轮已存在），本轮未动。

回滚：宿主机 `/tmp/synie_frag_backup_20260814T053158Z.sql`（两表 data-only）。

### 核验

| 抽查 | 结果 |
|---|---|
| `销-250122001` / 2000 / `F(P)58-87` | 仍挂 `简道云单号:SO20241223304` |
| `销-250122001` / 505 / `F(P)58-134` | 仍挂 `简道云单号:SO20250117531` |
| `SO20241223304` 已发 | 2289 → **2000**（`销-2112040007` 的 289 无 UUID，改挂客户 58 留档） |
| `销-230727013` | 29 行全挂回 UUID 目标 `SO20230720358` / `SO20230721373` / `SO20230715312`；拆片数量合计 = 旧应用 15 行（3600/8220/1000/600/260/200…）；不再挂 2023-11 以后的真单 |
| `销-230610002` | 29 行挂在旧应用 5 张目标单 `SO20230602868`–`872`，合计 17753 |
| `销-230506002` | 26 行挂 `SO20230407291` / `SO20230413370`；1 行（3000）无对上的 UUID 目标，改挂客户 1051 留档；合计 25236 不丢 |
| `销-2003300001`（Excel 代） | 仍挂客户 1006 留档，未补造订单 |
| `shipped_qty` 与发货合计 | 漂移 0 |

未做：流水重洗、采购/对账/库存期初、恢复 D5 容差、改「发货必挂订单」、给 Excel 代补造订单、物理合并拆行（行数仍高于简道云 43623）。5700 行超发不要再清。193 条无留档客户的碎片按拍板跳过。

## 13. 2026-08-14 补回出库缺行

抽样对账发现：发货**头**与简道云 22218 张一一对应，但部分历史 `销-` 在当初导入时只进了能映射的行，已存在的发货头后来没再补。本轮只往**原发货单**加行，不新建扩容单、不改订购 `qty`、不过账。

脚本：`.scratch/jdy-migration/scripts/list_missing_ship_lines.py`、`fill_missing_ship_lines.py`。对照：`rewash/fill_inserts.csv`、`fill_stats.json`。回滚：`/tmp/synie_fill_backup_20260814T065138Z.sql`。

| 项 | 数量 |
|---|---:|
| 应补 / 已补 | **2131 / 2131**（跳过 0） |
| 挂 UUID 目标 / 留档 / C 代 lookup | 1555 / 575 / 1 |
| 物料对上 `F(P)` / HIST 快照 | 1254 / 877 |
| 发货头 / 行 | 22218 / **51887**（49756+2131） |
| 补后 22218 张出库 Σqty | **与简道云 `ship_items` 全部一致**（短 0 / 超 0） |

挂法：有旧应用 UUID 的挂回目标订单行；`W(SO)-` 走新应用 lookup（本轮 1 行 `W(SO)-260624-0009` / 9800 / `F(P)241-1` → `SO-260606-0005`）；无 UUID 的挂该客户留档。`F(HIST)-UNMAPPED` 仍不挂真单。

核验：`销-240122011` 补回 `F(P)31-8`×1100；`销-230809005` 补回 50-23×1065 与铜件 1500；`销-250122001` 两行挂接不变；`SO20241223304` 已发仍为 2000；抽样 31/50/1111/13/1022 客户出库 Σqty 与简道云一致；`shipped_qty` 漂移 0。

未做：过账补库存分录（所补行与既有 SQL 台账出库一样不过账）。拆行合并见 §14。

## 14. 2026-08-14 按简道云 1:1 合并拆行

**口径**：除最早 Excel 代外，简道云出库行与订单行是严格一行对一行。导入 FIFO 把一行拆成多行，只是迁入形态，不是业务事实。本轮在原发货单内把能凑回简道云该行数量的拆片合并成一行（改主行 `qty`、删其余），不改订购数量、不过账、不动 `W(SO)-` / `XSFH`。

脚本：`.scratch/jdy-migration/scripts/merge_split_lines.py`。回滚：`/tmp/synie_merge_backup_20260814T080413Z.sql`。

| 项 | 数量 |
|---|---:|
| 已是 1:1、不动 | 26271 |
| 合并组 / 删掉的拆片 | **5579 / 6789** |
| 凑不回（名称规格对不上等） | 1284 |
| 发货头 / 行 | 22218 / **45098**（51887−6789；简道云出库行 43623） |
| 22218 张出库 Σqty | 仍与简道云全部一致 |

核验：`销-230727013` 29→**15** 行（与旧应用应有行一致），合计 19060；`销-250122001` 仍 2 行且挂接不变；`SO20241223304` 已发仍 2000；`销-2003300001` Excel 代 2 行未动；`F(HIST)-UNMAPPED` 不挂真单；`shipped_qty` 漂移 0。

未做：把凑不回的 1284 组再猜合并、过账、改订购数量。

### 14.1 写法不同再合并一波（同日）

图号忽略大小写 / `F(P)` / `Φ`，括号备注、`组件`、`新图纸` 不挡同一货。不同图号（`95-13` vs `95-31`）仍不合并。

| 项 | 数量 |
|---|---:|
| 本波合并组 / 删拆片 | 249 / 373 |
| 发货行 | 45098 → **44748**（含下面补回 23 行） |
| 仍凑不回 | 593（真不同规格，或数量已被别的行用掉） |

例：`销-230515014` 2300+3700 → 1 行 6000；`销-230518002` 三片 → 1 行 4800；`销-230520003` `77-1068A.02` / `53*60` 与 `F(P)77-1068a.02` / `Φ53*60` 认成同一行。

按物料编号合计时曾把同编号不同规格（圆帽 1" / 2"、两张同名 5000）合成一行，10 张出库 Σqty 变短。已把差额 23 行补回原单，22218 张数量再次与简道云一致；规则改为「只剩一条应有行，或规格折叠后相同」才按编号合计。补回时 2 行 HIST 物料误挂真单，已改挂留档。

### 14.2 差额行 + 图号写法再洗（同日）

按上次分类：把「差额」行补回能认出的简道云行；`50-(141.5*280)` 这类图号与 `F(P)50-1` 且规格相同的合并。同名不同尺寸、数量碰巧一样的仍不合。

| 项 | 数量 |
|---|---:|
| 本波合并（差额补回为主） | 449 组 / 删 587 |
| 发货行 | **44162**（简道云 43623） |
| 仍凑不回 | ~217 |
| 差额行残留 | 48 |

`销-2006230013` 925 差额 + 640 `F(P)4-17` → 1 行 1565。误把 `销-2006150001` 的 5130 收掉，已补回留档；3 行 HIST 物料改挂留档。22218 张 Σqty 再次与简道云一致。

## 15. 2026-08-15 48 条「差额」发货行对回简道云出库行

承接 §13–§14.2。补缺量时，发货单 Σqty 已与简道云对齐，但对不回具体行的数量写成了占位行 `material_name = (差额，未能对到具体简道云行)`。货在原单上，只是没标成简道云那一行。

本轮只 SQL 改快照 / 改挂 / 重算 `shipped_qty`：不改订购 `qty`、不过账、不新建扩容单、不动 `W(SO)-` / `XSFH`。同名不同尺寸不合；空白差额禁止 FIFO 拆到多个规格。

脚本：`.scratch/jdy-migration/scripts/remap_gap_lines.py`（默认 dry-run，`--export` 重拉线上表，`--apply` 先 dump 再写库）。对照：`rewash/gap48_*.csv`、`gap48_stats.json`、`gap48_check.json`。回滚：`/tmp/synie_gap48_backup_20260815T074517Z.sql`。

### 算法

对每张仍含差额行的 `销-` 发货单：

1. 应有行 = 旧应用该出库行；没有旧单则用 `raw/ship_items.json`。
2. 非差额、非「简道云未映射物料」占位行按规格折叠配对（两边都有规格则必须相同；碎片合计还要求名称相同，避免 120 目并进「单层」）。
3. 差额行填未盖满缺口：数量相等且唯一 → 改快照+改挂；只剩一个缺口 → 整行给它（数量保持差额原值）；多种组合都能凑满 → 不拆（避免 FIFO）。
4. 对不上：留在该客户留档，名称改成简道云残留快照，禁止再叫「差额」。
5. `F(HIST)-UNMAPPED` 不挂真单；有 UUID 目标且能解析到 `F(P)` 才改挂真订单行。
6. 全量 `shipped_qty = Σ` 仍挂在该行上的发货数量。

### 执行结果（线上，2026-08-15）

| 项 | 数量 |
|---|---:|
| 含差额的 `销-*` 发货单 / 差额行 | **48 / 48** |
| 数量相等对回应有行 | 18 |
| 唯一缺口对回（数量保留差额原值） | 19 |
| 留档改名（多缺口对不唯一，不再叫差额） | 11 |
| 拆行 / 并进主行 | 0 / 0 |
| 改挂真订单（UUID） / 留档 | 21 / 16 |
| 发货头 / 行 | **22218 / 44162**（不变） |
| `material_name LIKE '(差额%'` | **0** |
| `F(HIST)-UNMAPPED` 挂真单 | **0** |

A 类（已有 `F(P)`/规格、挂真单，只是名字叫差额）11 条全部改回简道云应有行名称/规格，必要时改挂该单 UUID 目标订单行。例：

| 简道云出库 | 原差额快照 | 对回 |
|---|---|---|
| 销-230710006 | 160 / `F(P)1051-16` / 外径18*34 | 过滤网1"（1*2网） / 外径26*32 → `SO20230628132` |
| 销-240701013 | 1200 / `F(P)1013` / 1-1/4" | 过滤网304（2*3小孔） / 1-1/2"（42.5*54） → `SO20240605367` |
| 销-221212002 | 800 / `F(P)1004` / 20.5*39 | 304过滤网1*2 / 26*44 → `SO20221110995` |

B 类大额：`销-2011050002` 78000 落到 11-1077 / `0D10H0.4`（客户 1006 留档，无 UUID）；`销-220419004` 40110 落到 单层滤网304 / 内Φ13.5*8.5 → `SO20220407105`；`销-231225007` 23990 两缺口（24000 单层316 + 1200 单层304）对不唯一，留档并改名为单层滤网316 / 内Φ13.5*8.5。

11 条留档原因都是「同一张单上剩多个未盖满应有行，差额数量不能唯一拆尽」，不是数量丢失。

### 核验

| 抽查 | 结果 |
|---|---|
| 22218 张 `简道云出库:` Σqty vs `raw/ship_items.json` | 短 0 / 超 0 |
| 旧应用 9489 张订购 `qty` | **9489/9489**，本轮未改 |
| `销-250122001` | 2000/`F(P)58-87`→`SO20241223304`，505/`F(P)58-134`→`SO20250117531` |
| `SO20241223304` 已发 | 仍为 **2000** |
| `shipped_qty` 与发货合计 | 漂移 0 |
| 客户 **1013 讯智** 出库 | 153/153 头，Σqty **2 192 960** 与简道云一致 |
| 1013 旧应用真单订购数量 | 72/72 一致；已发漂移 0 |

本轮动到的 1013 发货单 18 张（含 A 类 `销-221025005` / `销-230715001` / `销-230828001` / `销-240406017` / `销-240701013` / `销-240827013`）。

未做：采购/对账/库存期初、恢复 D5 容差、给 Excel 代补造订单、为消超发改订购数量、把不同规格合成一行。银行流水已于 2026-08-15 按清洗表覆盖，见 §16。

## 16. 2026-08-15 银行流水按清洗表覆盖

事实源：`~/银行流水.xlsx`（清洗后流水第 4 版，6472 行）。线上原 6299 笔无对账、无导入批次。

策略：`occurred_at <= 2026-07-02 07:12:25` 全删（6239），该时点之后 60 笔（7 月续记）保留；SQL `COPY` 导入清洗表。旧行备份在库表 `_bank_tx_bak_20260815`。

| 账户 | 原线上 | 清洗表 | 截止日后保留 | 现线上 |
|---|---:|---:|---:|---:|
| F(BA)-00001 京泰工商 | 2947 | 2937 | 40 | **2977** |
| F(BA)-00002 京泰宁波 | 970 | 1049 | 4 | **1053** |
| F(BA)-00003 东方农行 | 2275 | 2369 | 16 | **2385** |
| F(BA)-00004 东方农商 | 46 | 55 | 0 | **55** |
| F(BA)-00005 京泰宁波 USD | 3 | 3 | 0 | **3** |
| F(BA)-00006 京泰工商 USD | 58 | 59 | 0 | **59** |
| 合计 | 6299 | 6472 | 60 | **6532** |

清洗表相对原简道云去重载荷的主要订正（已写入）：重复删除 5、方向/符号 39、金额解析 2（含 2024-10-15 借款 100 万被记成 1 千）、余额重算 98、错分银行 5（承兑入金从 00001 改到 00002）、备份补入 10（2021 宁波早期）、对账单补入 1（2022-01-29 农行 福建山牛垰 10,000）。

清洗表仍标出的缺口（未编造、未补）：00001 净差约 -34,175（2020 年末孤立行）、00004 净差 +47,864.95（2023-06 疑漏记）。00002/00005/00006 余额链已对平。
