# ADR：自动编号规则

2026-07-10。单据号由 **编号规则**（`sys_numbering_rule`）生成：固定文本 / 记录字段 / 序号的有序段组合；每单据类型至多一条启用规则。

- 序号按段渲染结果（及公司）隐含范围独立计数，无独立「重置周期」实体。
- 字段段取值为空时**省略该段**（不报错）；序号 `padding=0` 表示不补零（见后续 ADR 细化）。
- 留空可自动取号，多数单据仍允许手填覆盖（按资源约定）。
