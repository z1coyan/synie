# ADR：银行对账挂来源单据（多态凭证）

2026-08-18。银行对账记录勾稽的是**总账来源单据**（`voucher_type` + `voucher_id`，与分录同一套多态引用），不再只外键手工凭证。

贴现/兑付审核过账后，流水应对到**该承兑交易**的银行科目分录，不得再为同一笔现金新建手工凭证。容量按未作废 `acc_gl_entry` 该银行科目该方向合计计算。

否决「贴现单另挂 journal_id」：会有两张凭证、利息与票面拆在两处。
