# 销售订单对手采用 Party 多态（客户/内部公司）

销售订单的「客户」字段不直接 belongs_to 客户主数据，而是复用凭证行/发票同款的 Party 多态引用（`party_type` + `party_id`，无真外键），取值限客户/内部公司两类——为集团内公司互售留门（内部互开发票已有「对向发票」先例），代价是没有真外键、存在性靠 `PartyExists` 校验兜底。直接挂客户主数据的方案被否：将来加内部公司时需迁移存量数据，而多态方案届时无需改动。供应商被明确排除（留给将来的采购订单），由资源校验拦截而非扩展 PartyType 枚举。
