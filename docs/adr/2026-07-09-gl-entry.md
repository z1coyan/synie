# ADR：总账分录为唯一财务事实表

2026-07-09。**总账分录**（`acc_gl_entry`）只追加不可改；由来源单据审核派生，报表只查分录不查单据。作废=标记 `is_cancelled`，不删行。

- **红冲**（红字冲销）是已入账更正方式：原组分录取负对冲并标记已被红冲，名下合计归零。
- **对手**（Party）多态引用挂在需要往来的分录上；挂往来角色科目的分录必须填对手（红字行豁免）。
- 手工凭证与业务单据共用同一事实表语义。
