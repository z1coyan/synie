# ADR：存储接入点与文件挂接

2026-07-14。文件字节落在**存储接入点**（local / S3 / OSS），全局唯一默认；新上传落默认点。配置从库表读取（退役 config 里写死的 storages 列表）。

- **文件挂接**关联业务记录；有挂接的文件不可删；下载授权按宿主可见性判定。
- 内置 local 接入由初始化/seed 幂等写入；默认接入与内置接入删除受保护。
