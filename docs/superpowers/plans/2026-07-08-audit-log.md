# 审计日志(sys_audit_log)实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 单表 `sys_audit_log` 审计日志:一个通用 Change 记录所有受审计资源的增/改/删字段级变更,GraphQL 一个只读 query 同时支撑详情页编辑记录与全局审计页。

**Architecture:** 新增 `SynieCore.Audit` 命名空间三个模块——`Log`(Postgres 资源,只读对外)、`Track`(通用 `Ash.Resource.Change`,after_action 内 diff 新旧值同事务写日志)、`Fragment`(Spark DSL fragment,资源一行接入)。现有 8 个可写资源挂 fragment 并将 update/destroy 动作设 `require_atomic? false`。

**Tech Stack:** Elixir 1.20 / Ash 3.29 / AshPostgres 2.10 / AshGraphql 1.9(全部已安装,不加新依赖)

**Spec:** `docs/superpowers/specs/2026-07-08-audit-log-design.md`

## Global Constraints

- 第一语言中文:moduledoc、注释、commit message 均中文。
- 所有命令在 `backend/` 目录执行;mix 不在非交互 PATH,每个 shell 先执行:
  `export PATH="$HOME/.elixir-install/installs/otp/28.4/bin:$HOME/.elixir-install/installs/elixir/1.20.2-otp-28/bin:$PATH"`
- Postgres 在 5440 端口(`synie-pg` 容器),dev/test 配置已就位,无需传 PGPORT。
- 权限照样板 `apps/synie_core/test/support/test_domain.ex`(Test.Doc):authorizer + 三段 policies;权限码惯例 `sys.audit_log:read`。
- `authorize?: false` 仅限受信内部路径(审计写入、测试夹具)。
- commit message 结尾加 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- 测试命令:`mix test`(umbrella 根即 backend/);单文件 `mix test apps/synie_core/test/...`。

## File Structure

| 文件 | 职责 |
|---|---|
| `apps/synie_core/lib/synie_core/audit/log.ex` | 新建:审计日志资源(表 `sys_audit_log`),只读对外 + 内部 `:record` create |
| `apps/synie_core/lib/synie_core/audit/track.ex` | 新建:通用审计 Change(diff + 写日志) |
| `apps/synie_core/lib/synie_core/audit/fragment.ex` | 新建:接入 fragment(一个 changes 块) |
| `apps/synie_core/lib/synie_core.ex` | 修改:注册资源 + GraphQL list query |
| `apps/synie_core/lib/synie_core/base/currency.ex` 等 8 个资源 | 修改:挂 fragment + `require_atomic? false` |
| `apps/synie_core/test/synie_core/audit/log_test.exs` | 新建:读权限/公司范围测试 |
| `apps/synie_core/test/synie_core/audit/track_test.exs` | 新建:diff 行为测试 |
| `apps/synie_core/test/synie_core/authz/registry_test.exs` | 修改:权限目录期望加 `sys.audit_log` |
| `apps/synie_core/priv/repo/migrations/*_add_sys_audit_log.exs` | codegen 生成,勿手写 |

---

### Task 1: `Audit.Log` 资源、迁移与读权限

**Files:**
- Create: `apps/synie_core/lib/synie_core/audit/log.ex`
- Create: `apps/synie_core/test/synie_core/audit/log_test.exs`
- Modify: `apps/synie_core/lib/synie_core.ex`(resources 块 + graphql queries 块)
- Modify: `apps/synie_core/test/synie_core/authz/registry_test.exs:11`(期望列表加 `sys.audit_log`)
- 生成: 迁移文件(`mix ash.codegen`)

**Interfaces:**
- Produces: `SynieCore.Audit.Log` 资源;内部写入动作 `:record`,accept `[:resource, :record_id, :record_label, :action_type, :action_name, :actor_id, :actor_name, :company_id, :changes]`;`permission_prefix/0` = `"sys.audit_log"`。Task 2 的 Track 调用 `:record`。

- [ ] **Step 1: 写失败测试**

`apps/synie_core/test/synie_core/audit/log_test.exs`:

```elixir
defmodule SynieCore.Audit.LogTest do
  use ExUnit.Case, async: true

  alias SynieCore.Audit.Log
  alias SynieCore.Authz.Actor

  @co_a Ash.UUID.generate()
  @co_b Ash.UUID.generate()

  setup do
    :ok = Ecto.Adapters.SQL.Sandbox.checkout(SynieCore.Repo)
  end

  defp log!(attrs) do
    Log
    |> Ash.Changeset.for_create(
      :record,
      Map.merge(
        %{
          resource: "bas_currency",
          record_id: Ash.UUID.generate(),
          action_type: "update",
          action_name: "update",
          changes: %{"name" => %{"from" => "a", "to" => "b"}}
        },
        attrs
      )
    )
    |> Ash.create!(authorize?: false)
  end

  defp reader(overrides \\ %{}) do
    struct!(
      %Actor{user_id: Ash.UUID.generate(), permissions: MapSet.new(["sys.audit_log:read"])},
      overrides
    )
  end

  test "资源声明了权限前缀" do
    assert Log.permission_prefix() == "sys.audit_log"
    assert Log.permission_actions() == ~w(read)
  end

  test "无权限码读取被拒绝" do
    assert {:error, %Ash.Error.Forbidden{}} =
             Ash.read(Log, actor: %Actor{user_id: Ash.UUID.generate()})
  end

  test "全局资源日志(company_id 为空)有权限码即可见" do
    log!(%{})

    assert [_] = Ash.read!(Log, actor: reader())
  end

  test "公司日志 fail-closed:空 company_id 行 + 授权公司行可见" do
    log!(%{})
    log!(%{company_id: @co_a})
    log!(%{company_id: @co_b})

    assert length(Ash.read!(Log, actor: reader(%{company_ids: [@co_a]}))) == 2
    assert [_] = Ash.read!(Log, actor: reader())
  end

  test "super_admin 全可见" do
    log!(%{})
    log!(%{company_id: @co_a})

    admin = reader(%{super_admin: true, permissions: MapSet.new()})

    assert length(Ash.read!(Log, actor: admin)) == 2
  end
end
```

- [ ] **Step 2: 跑测试确认失败**

```bash
mix test apps/synie_core/test/synie_core/audit/log_test.exs
```

预期:编译失败,`SynieCore.Audit.Log is undefined`。

- [ ] **Step 3: 实现资源并注册**

`apps/synie_core/lib/synie_core/audit/log.ex`:

```elixir
defmodule SynieCore.Audit.Log do
  @moduledoc """
  审计日志,对应 `sys_audit_log` 表。

  仅由 `SynieCore.Audit.Track` 经 `:record` 动作内部写入(`authorize?: false`),
  对外只读;只增不改不删。`changes` 格式:`%{"字段" => %{"from" => 旧, "to" => 新}}`。
  """

  use Ash.Resource,
    domain: SynieCore,
    data_layer: AshPostgres.DataLayer,
    extensions: [AshGraphql.Resource],
    authorizers: [Ash.Policy.Authorizer]

  postgres do
    table "sys_audit_log"
    repo SynieCore.Repo

    custom_indexes do
      index [:resource, :record_id, :inserted_at]
      index [:inserted_at]
    end
  end

  graphql do
    type :sys_audit_log
  end

  policies do
    bypass actor_attribute_equals(:super_admin, true) do
      authorize_if always()
    end

    policy always() do
      authorize_if SynieCore.Authz.Checks.HasPermission
    end

    # 公司日志按授权公司过滤(fail-closed);全局资源日志(company_id 为空)不受限
    policy action_type(:read) do
      authorize_if expr(is_nil(company_id))
      authorize_if SynieCore.Authz.Checks.CompanyScope
    end
  end

  def permission_prefix, do: "sys.audit_log"
  def permission_actions, do: ~w(read)

  actions do
    read :read do
      primary? true
      pagination offset?: true, countable: true, required?: false, default_limit: 50
      prepare build(sort: [inserted_at: :desc])
    end

    # 仅供 Audit.Track 内部调用,不暴露 GraphQL mutation
    create :record do
      accept [
        :resource,
        :record_id,
        :record_label,
        :action_type,
        :action_name,
        :actor_id,
        :actor_name,
        :company_id,
        :changes
      ]
    end
  end

  attributes do
    uuid_primary_key :id

    attribute :resource, :string do
      allow_nil? false
      public? true
    end

    attribute :record_id, :uuid do
      allow_nil? false
      public? true
    end

    # 记录展示名(资源的 name 属性),记录删除后仍可显示
    attribute :record_label, :string do
      public? true
    end

    attribute :action_type, :string do
      allow_nil? false
      public? true
    end

    attribute :action_name, :string do
      allow_nil? false
      public? true
    end

    attribute :actor_id, :uuid do
      public? true
    end

    attribute :actor_name, :string do
      public? true
    end

    attribute :company_id, :uuid do
      public? true
    end

    attribute :changes, :map do
      allow_nil? false
      public? true
    end

    create_timestamp :inserted_at
  end
end
```

`apps/synie_core/lib/synie_core.ex` 两处修改。queries 块末尾加:

```elixir
list SynieCore.Audit.Log, :sys_audit_logs, :read, paginate_with: :offset
```

resources 块末尾加:

```elixir
resource SynieCore.Audit.Log
```

`apps/synie_core/test/synie_core/authz/registry_test.exs` 第 11 行左右,期望列表加 `sys.audit_log`:

```elixir
assert prefixes ==
         Enum.sort(
           ~w(sys.role sys.user_role sys.role_permission sys.user_company sys.audit_log org.company base.unit base.currency)
         )
```

- [ ] **Step 4: 生成并执行迁移**

```bash
mix ash.codegen add_sys_audit_log
mix ash.migrate
MIX_ENV=test mix ash.migrate
```

预期:生成 `apps/synie_core/priv/repo/migrations/*_add_sys_audit_log.exs`(含 `sys_audit_log` 表与两个索引),dev/test 库各执行成功。检查生成的迁移内容确认表名、jsonb 列、索引无误。

- [ ] **Step 5: 跑测试确认通过**

```bash
mix test apps/synie_core/test/synie_core/audit/log_test.exs apps/synie_core/test/synie_core/authz/registry_test.exs
```

预期:全部 PASS。

- [ ] **Step 6: 提交**

```bash
git add -A && git commit -m "feat: 审计日志资源 sys_audit_log(只读 + 权限/公司范围过滤)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: `Audit.Track` + `Audit.Fragment`,试点接入 `bas_currency`

**Files:**
- Create: `apps/synie_core/lib/synie_core/audit/track.ex`
- Create: `apps/synie_core/lib/synie_core/audit/fragment.ex`
- Create: `apps/synie_core/test/synie_core/audit/track_test.exs`
- Modify: `apps/synie_core/lib/synie_core/base/currency.ex`(use 行 + actions 块)

**Interfaces:**
- Consumes: Task 1 的 `SynieCore.Audit.Log` `:record` 动作。
- Produces: `SynieCore.Audit.Fragment`(资源经 `fragments: [SynieCore.Audit.Fragment]` 接入);changes JSON 约定 create=`{"to"}`、update=`{"from","to"}`、destroy=`{"from"}`,敏感属性值为 `"[FILTERED]"`。Task 3 依赖此接入方式。

- [ ] **Step 1: 写失败测试**

`apps/synie_core/test/synie_core/audit/track_test.exs`:

```elixir
defmodule SynieCore.Audit.TrackTest do
  use ExUnit.Case, async: true

  alias SynieCore.Audit.Log
  alias SynieCore.Authz.Actor
  alias SynieCore.Base.Currency

  setup do
    :ok = Ecto.Adapters.SQL.Sandbox.checkout(SynieCore.Repo)
  end

  defp actor do
    %Actor{user_id: Ash.UUID.generate(), username: "auditor", permissions: MapSet.new()}
  end

  defp currency!(attrs, opts \\ []) do
    Currency
    |> Ash.Changeset.for_create(
      :create,
      Map.merge(%{name: "美元", iso_code: "USD", symbol: "$"}, attrs)
    )
    |> Ash.create!(Keyword.merge([authorize?: false], opts))
  end

  defp logs_for(record_id) do
    Log
    |> Ash.Query.filter_input(%{record_id: record_id})
    |> Ash.read!(authorize?: false)
  end

  test "create 落审计日志(含操作人与展示名)" do
    a = actor()
    currency = currency!(%{}, actor: a)

    assert [log] = logs_for(currency.id)
    assert log.resource == "bas_currency"
    assert log.action_type == "create"
    assert log.action_name == "create"
    assert log.record_label == "美元"
    assert log.actor_id == a.user_id
    assert log.actor_name == "auditor"
    assert log.changes["iso_code"] == %{"to" => "USD"}
    refute Map.has_key?(log.changes, "id")
    refute Map.has_key?(log.changes, "inserted_at")
  end

  test "无 actor 的内部写入 actor 字段为空" do
    currency = currency!(%{})

    assert [log] = logs_for(currency.id)
    assert log.actor_id == nil
    assert log.actor_name == nil
  end

  test "update 只记录变更字段的 from/to" do
    currency = currency!(%{})

    currency
    |> Ash.Changeset.for_update(:update, %{name: "美刀"})
    |> Ash.update!(authorize?: false)

    assert [log] = Enum.filter(logs_for(currency.id), &(&1.action_type == "update"))
    assert log.changes == %{"name" => %{"from" => "美元", "to" => "美刀"}}
  end

  test "无实际变更的 update 不落日志" do
    currency = currency!(%{})

    currency
    |> Ash.Changeset.for_update(:update, %{name: "美元"})
    |> Ash.update!(authorize?: false)

    assert Enum.filter(logs_for(currency.id), &(&1.action_type == "update")) == []
  end

  test "destroy 记录删除前快照" do
    currency = currency!(%{})

    currency
    |> Ash.Changeset.for_destroy(:destroy)
    |> Ash.destroy!(authorize?: false)

    assert [log] = Enum.filter(logs_for(currency.id), &(&1.action_type == "destroy"))
    assert log.changes["name"] == %{"from" => "美元"}
    assert log.changes["iso_code"] == %{"from" => "USD"}
    assert log.record_label == "美元"
  end

  test "业务动作失败时不留审计日志" do
    assert_raise Ash.Error.Invalid, fn -> currency!(%{iso_code: "usd"}) end

    assert Ash.read!(Log, authorize?: false) == []
  end
end
```

- [ ] **Step 2: 跑测试确认失败**

```bash
mix test apps/synie_core/test/synie_core/audit/track_test.exs
```

预期:FAIL——currency create 后查不到日志(`assert [log] =` 匹配失败,得到 `[]`)。

- [ ] **Step 3: 实现 Track 与 Fragment,接入 Currency**

`apps/synie_core/lib/synie_core/audit/track.ex`:

```elixir
defmodule SynieCore.Audit.Track do
  @moduledoc """
  通用审计 change:after_action 中 diff 旧值(`changeset.data`)与动作结果,
  经 `SynieCore.Audit.Log` 的 `:record` 动作同事务写入审计日志。

  - 覆盖全部属性(含非 public),仅跳过主键与时间戳
  - `sensitive? true` 属性记 `"[FILTERED]"`
  - update 无实际变更(no-op)不落日志
  - 接入方式见 `SynieCore.Audit.Fragment`
  """

  use Ash.Resource.Change

  alias SynieCore.Authz.Actor

  @skip_attrs [:id, :inserted_at, :updated_at]
  @filtered "[FILTERED]"

  @impl true
  def change(changeset, _opts, context) do
    Ash.Changeset.after_action(changeset, fn changeset, result ->
      write_log(changeset, result, context)
      {:ok, result}
    end)
  end

  defp write_log(changeset, result, context) do
    action_type = changeset.action_type
    changes = build_changes(action_type, changeset, result)

    if action_type == :update and changes == %{} do
      :ok
    else
      # 删除动作的属性取值来源是删除前数据
      source = if action_type == :destroy, do: changeset.data, else: result

      SynieCore.Audit.Log
      |> Ash.Changeset.for_create(:record, %{
        resource: resource_name(changeset.resource),
        record_id: result.id,
        record_label: record_label(changeset.resource, source),
        action_type: to_string(action_type),
        action_name: to_string(changeset.action.name),
        actor_id: actor_id(context.actor),
        actor_name: actor_name(context.actor),
        company_id: company_id(changeset.resource, source),
        changes: changes
      })
      # 受信内部路径:审计写入不做权限检查
      |> Ash.create!(authorize?: false)

      :ok
    end
  end

  defp build_changes(:create, changeset, result) do
    collect(changeset.resource, fn attr ->
      case Map.get(result, attr.name) do
        nil -> nil
        value -> %{"to" => dump(attr, value)}
      end
    end)
  end

  defp build_changes(:update, changeset, result) do
    collect(changeset.resource, fn attr ->
      old = Map.get(changeset.data, attr.name)
      new = Map.get(result, attr.name)

      if old != new do
        %{"from" => dump(attr, old), "to" => dump(attr, new)}
      end
    end)
  end

  defp build_changes(:destroy, changeset, _result) do
    collect(changeset.resource, fn attr ->
      case Map.get(changeset.data, attr.name) do
        nil -> nil
        value -> %{"from" => dump(attr, value)}
      end
    end)
  end

  defp collect(resource, fun) do
    resource
    |> Ash.Resource.Info.attributes()
    |> Enum.reject(&(&1.name in @skip_attrs))
    |> Enum.reduce(%{}, fn attr, acc ->
      case fun.(attr) do
        nil -> acc
        entry -> Map.put(acc, to_string(attr.name), entry)
      end
    end)
  end

  defp dump(%{sensitive?: true}, _value), do: @filtered
  defp dump(_attr, value), do: normalize(value)

  # jsonb 可编码值:标量直存,常见结构转字符串,兜底 inspect
  defp normalize(v) when is_binary(v) or is_number(v) or is_boolean(v) or is_nil(v), do: v
  defp normalize(%Ash.CiString{} = v), do: to_string(v)
  defp normalize(%Decimal{} = v), do: Decimal.to_string(v)
  defp normalize(%DateTime{} = v), do: DateTime.to_iso8601(v)
  defp normalize(%NaiveDateTime{} = v), do: NaiveDateTime.to_iso8601(v)
  defp normalize(%Date{} = v), do: Date.to_iso8601(v)
  defp normalize(%_{} = v), do: inspect(v)
  defp normalize(v) when is_atom(v), do: to_string(v)
  defp normalize(v) when is_list(v), do: Enum.map(v, &normalize/1)
  defp normalize(v) when is_map(v), do: Map.new(v, fn {k, val} -> {to_string(k), normalize(val)} end)

  # resource 标识用 GraphQL type 名(与前端天然对齐),无 graphql 扩展时退回表名
  defp resource_name(resource) do
    case AshGraphql.Resource.Info.type(resource) do
      nil -> AshPostgres.DataLayer.Info.table(resource)
      type -> to_string(type)
    end
  end

  defp record_label(resource, source) do
    if Ash.Resource.Info.attribute(resource, :name) do
      case Map.get(source, :name) do
        nil -> nil
        label -> to_string(label)
      end
    end
  end

  defp company_id(resource, source) do
    if Ash.Resource.Info.attribute(resource, :company_id) do
      Map.get(source, :company_id)
    end
  end

  defp actor_id(%Actor{user_id: id}), do: id
  defp actor_id(_), do: nil

  defp actor_name(%Actor{username: name}) when not is_nil(name), do: to_string(name)
  defp actor_name(_), do: nil
end
```

`apps/synie_core/lib/synie_core/audit/fragment.ex`:

```elixir
defmodule SynieCore.Audit.Fragment do
  @moduledoc """
  资源接入审计的唯一入口:

      use Ash.Resource,
        ...,
        fragments: [SynieCore.Audit.Fragment]

  注意:受审计资源的 update/destroy 动作需 `require_atomic? false`
  (after_action 钩子无法原子执行,Ash 3 默认动作原子化);
  批量操作须用 `strategy: :stream`,`:atomic` 会绕过审计。
  """

  use Spark.Dsl.Fragment, of: Ash.Resource

  changes do
    change SynieCore.Audit.Track, on: [:create, :update, :destroy]
  end
end
```

`apps/synie_core/lib/synie_core/base/currency.ex` 两处修改。use 行加 fragments:

```elixir
  use Ash.Resource,
    domain: SynieCore,
    data_layer: AshPostgres.DataLayer,
    extensions: [AshGraphql.Resource],
    authorizers: [Ash.Policy.Authorizer],
    fragments: [SynieCore.Audit.Fragment]
```

actions 块整体替换为(default destroy 换成显式声明以设 `require_atomic? false`,`primary? true` 保持原默认动作语义):

```elixir
  actions do
    defaults [:read]

    create :create do
      accept [:name, :iso_code, :symbol]
    end

    update :update do
      accept [:name, :symbol]
      require_atomic? false
    end

    destroy :destroy do
      primary? true
      require_atomic? false
    end
  end
```

备用方案:若 `fragments:` 中的 `changes` 块遇到 Spark 边角问题编译不过,退化为在每个资源内直接写 `changes do change SynieCore.Audit.Track, on: [:create, :update, :destroy] end`(每资源 3 行),Fragment 模块删除,计划其余不变。

- [ ] **Step 4: 跑测试确认通过**

```bash
mix test apps/synie_core/test/synie_core/audit/track_test.exs
```

预期:全部 PASS。同时跑一遍既有货币测试防回归:

```bash
mix test apps/synie_core/test/synie_core/base/currency_test.exs
```

- [ ] **Step 5: 提交**

```bash
git add -A && git commit -m "feat: 通用审计 Change 与接入 Fragment,试点 bas_currency

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: 其余 7 个资源接入审计

**Files:**
- Modify: `apps/synie_core/lib/synie_core/base/unit.ex`
- Modify: `apps/synie_core/lib/synie_core/org/company.ex`
- Modify: `apps/synie_core/lib/synie_core/authz/role.ex`
- Modify: `apps/synie_core/lib/synie_core/authz/role_permission.ex`
- Modify: `apps/synie_core/lib/synie_core/authz/user_role.ex`
- Modify: `apps/synie_core/lib/synie_core/authz/user_company.ex`
- Modify: `apps/synie_core/lib/synie_core/accounts/user.ex`
- Test: `apps/synie_core/test/synie_core/audit/track_test.exs`(追加 3 个测试)

**Interfaces:**
- Consumes: Task 2 的 `SynieCore.Audit.Fragment` 与 changes JSON 约定。
- Produces: 无新接口;全部现有可写资源产生审计日志。

- [ ] **Step 1: 追加失败测试**

在 `track_test.exs` 模块内追加(顶部 `import SynieCore.AuthzFixtures`):

```elixir
  test "sensitive 属性记录为 [FILTERED]" do
    user = user!()

    assert [log] = logs_for(user.id)
    assert log.resource == "sys_user"
    assert log.action_name == "register"
    assert log.changes["hashed_password"] == %{"to" => "[FILTERED]"}
  end

  test "带 company_id 的资源日志冗余公司标识" do
    user = user!()
    company = company!()
    uc = grant_company!(user, company)

    assert [log] = logs_for(uc.id)
    assert log.resource == "sys_user_company"
    assert log.company_id == company.id
  end

  test "非 public 属性变更(set_super_admin)可审计" do
    user = user!()

    user
    |> Ash.Changeset.for_update(:set_super_admin, %{})
    |> Ash.update!(authorize?: false)

    assert [log] = Enum.filter(logs_for(user.id), &(&1.action_type == "update"))
    assert log.changes["super_admin"] == %{"from" => false, "to" => true}
  end
```

- [ ] **Step 2: 跑测试确认失败**

```bash
mix test apps/synie_core/test/synie_core/audit/track_test.exs
```

预期:新增 3 个测试 FAIL(查不到日志),原有测试 PASS。

- [ ] **Step 3: 逐资源接入**

7 个资源的 `use Ash.Resource` 都追加一行 `fragments: [SynieCore.Audit.Fragment]`(User 当前无 authorizers,仅加 fragments 行,不加 authorizer)。actions 块逐资源整体替换如下,除此以外不动:

**unit.ex**:

```elixir
  actions do
    defaults [:read]

    create :create do
      accept [:unit_type, :is_base, :name, :symbol, :ratio]
    end

    update :update do
      accept [:unit_type, :is_base, :name, :symbol, :ratio]
      require_atomic? false
    end

    destroy :destroy do
      primary? true
      require_atomic? false
    end
  end
```

**company.ex**:

```elixir
  actions do
    defaults [:read]

    create :create do
      accept [:code, :name, :short_name, :parent_id]
    end

    update :update do
      accept [:name, :short_name, :parent_id]
      require_atomic? false
    end

    destroy :destroy do
      primary? true
      require_atomic? false
    end
  end
```

**role.ex**:

```elixir
  actions do
    defaults [:read]

    create :create do
      accept [:code, :name, :enabled]
    end

    update :update do
      accept [:name, :enabled]
      require_atomic? false
    end

    destroy :destroy do
      primary? true
      require_atomic? false
    end
  end
```

**role_permission.ex**:

```elixir
  actions do
    defaults [:read]

    create :create do
      accept [:role_id, :permission]
    end

    destroy :destroy do
      primary? true
      require_atomic? false
    end
  end
```

**user_role.ex**:

```elixir
  actions do
    defaults [:read]

    create :create do
      accept [:user_id, :role_id]
    end

    destroy :destroy do
      primary? true
      require_atomic? false
    end
  end
```

**user_company.ex**:

```elixir
  actions do
    defaults [:read]

    create :create do
      accept [:user_id, :company_id]
    end

    destroy :destroy do
      primary? true
      require_atomic? false
    end
  end
```

**user.ex**(无 destroy;register/by_username 不动,set_super_admin 加一行):

```elixir
    update :set_super_admin do
      accept []
      require_atomic? false

      change set_attribute(:super_admin, true)
    end
```

- [ ] **Step 4: 跑全量测试确认通过**

```bash
mix test
```

预期:全部 PASS。重点关注 `policies_test.exs`(其 set_super_admin 用例现在会产生审计日志,但无相关断言,应不受影响)与 `resources_test.exs`。

- [ ] **Step 5: 提交**

```bash
git add -A && git commit -m "feat: 全部现有可写资源接入审计日志

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: 收尾——格式化、全量回归、spec 状态

**Files:**
- Modify: `docs/superpowers/specs/2026-07-08-audit-log-design.md`(状态行)

**Interfaces:** 无。

- [ ] **Step 1: 格式化并全量回归**

```bash
mix format
mix test
```

预期:format 无 diff 或仅本次新文件被整理;测试全部 PASS。若 `mix format` 产生改动,重跑 `mix test` 确认仍 PASS。

- [ ] **Step 2: GraphQL schema 冒烟**

```bash
mix compile --warnings-as-errors
```

预期:编译零警告(AshGraphql 在编译期校验 schema,`sys_audit_logs` query 与 `sys_audit_log` type 生成无冲突)。

- [ ] **Step 3: 更新 spec 状态并提交**

spec 首行 `日期:2026-07-08 状态:待评审` 改为 `日期:2026-07-08 状态:已实现`。

```bash
git add -A && git commit -m "chore: 审计日志收尾(格式化与 spec 状态)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```
