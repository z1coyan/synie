# 库存物料

## 概述

库存物料模块管理企业的物料分类体系，为采购、库存等供应链业务提供统一的物料归属框架，服务全体项目成员。分类全局共享、不分公司。当前已实现物料分类的维护；物料主数据、出入库、库存分录主干（Stock Ledger）均未实现，不在本文范围。

## 核心概念

- **物料分类**：物料的树形分类学，全局共享不分公司，层级深度不限。
- **叶子分类**：可挂物料的末级分类；叶子不能再有子分类。与科目的「汇总」语义相反，勿混。
- **分类编号**：人工定义、全局唯一的编码（如 01=原材料），将来作为物料编号前缀；可改，已生成的物料编号不追溯。
- **上级分类**：分类自引用构成树；不填即为根分类，允许多个根分类并列。
- **启用/停用**：分类的启停标记，仅作状态记录，不联动其他约束。

## 功能清单

### 分类新建与编辑

维护分类的编号、名称、是否叶子、启用状态与上级分类。

- 分类编号必填、全局唯一，重复时报「分类编号已存在」；编号不限格式，不要求继承父级前缀。
- 名称必填，不要求唯一。
- 新分类默认是叶子、默认启用；要建可挂子分类的中间分类，须手动把「叶子」关掉。
- 上级分类可空（即根分类）；不能选自身；只能选非叶子分类——叶子分类不能挂子分类。
- 编号与上级创建后均可改；分类编号将来拼入物料编号，改码不追溯已生成的物料编号。
- 已有子分类的分类不能改成叶子。
- 边界：暂不检测多级成环（如 A→B→A），属已知留跟进项。

### 分类删除

- 有下级分类的不能删，须先移走或删除全部下级。
- 删除即彻底删除，无作废/归档；操作留审计记录。

### 分类树浏览

- 入口：「供应链 → 物料 → 物料分类」页面。
- 树形展示、逐级懒加载展开，每层按分类编号升序。
- 全局共享：页面无公司选择器，所有公司看到同一棵分类树。
- 增删改查需具备物料分类的相应权限，操作接审计。

## 业务流程

本模块目前只有分类维护单一动线，无跨功能流程：先建中间分类（建时关掉「叶子」），再在其下逐级建子分类；调整层级通过编辑「上级分类」完成。

## 相关文档

- [物料分类设计](../superpowers/specs/2026-07-15-material-category-design.md)
- [ADR：物料分类](../adr/2026-07-15-material-category.md)
- [术语表](../glossary.md)
