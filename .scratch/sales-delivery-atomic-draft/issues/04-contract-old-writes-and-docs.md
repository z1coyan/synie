# 04 — 收缩细粒度写接口并同步文档

**What to build:** 在销售发货新建、编辑和保存审核流程全部迁移到整单写边界后，删除发货条目、装箱箱和装箱行的独立创建、修改、删除入口及前端写能力，使整单创建和整单替换成为销售发货草稿的唯一权威写路径。子资源查询与单条读取继续服务列表、查看和对账等消费面；整单删除、审核、作废保持原入口。同步产品文档与契约守卫，明确整单原子保存、完整快照删除和两阶段保存审核语义，并核对领域词汇表无需新增术语。

**Blocked by:** 03 — 失败定位与“保存并审核”闭环

**Status:** resolved

- [x] 发货条目独立创建、修改、删除服务入口、HTTP 写路由和前端调用全部移除
- [x] 装箱箱独立创建、删除服务入口、HTTP 写路由和前端调用全部移除
- [x] 装箱行独立创建、修改、删除服务入口、HTTP 写路由和前端调用全部移除
- [x] 子资源查询和单条读取仍可供发货条目列表、销售对账候选及抽屉查看使用
- [x] 整单删除、审核、作废、查询和读取行为与权限保持不变
- [x] 代码库不存在对已删除细粒度写入口的产品调用，类型化 REST 契约与构建通过
- [x] 契约测试确认整单创建和整单替换是销售发货抽屉唯一保存路径，旧写路由不可访问
- [x] 产品文档说明整单原子保存、缺失子记录即删除、失败整单回滚及“保存并审核”两阶段行为
- [x] 核对领域词汇表中的销售发货单、发货条目、装箱箱和装箱清单（行）与实现一致；没有新增领域术语时不改词条
- [x] 完整的服务端测试、前端测试、类型检查和销售发货端到端验收全部通过
