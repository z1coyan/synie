# 13 HR：考勤与薪酬

Status: done
Blocked by: 02

## 范围

1. **考勤导入**（.dat 解析凭考勤机编号匹配员工；导入批次留痕；勾选自动创建 [未知] 占位员工；(员工,时刻) 唯一）
2. **日考勤重算**（半天桶 12:00；桶内最早/最晚卡；30 分钟向下取整；缺卡语义；加班=下午段超 4h；奖励工日 ≥3.5h 加 0.5；补卡单合并取值，原始打卡永不修改）
3. **工资单**（(员工,月) 唯一快照；应发=工日×日薪+补贴+奖金−罚款−借款抵扣；已发放锁死）
4. **发放记录**（首笔翻转已发放/补发可负）与 **借款台账**（员工级余额；发放抵扣自动生成归还行）

## 行为参考

`server-go/internal/domain/hr/`；CONTEXT 考勤/工资词条（「半天桶」「奖励工日」「补卡单」等）。

## 验收

- `verify-hr-operations-rest.ts` 全绿
- 重算纯函数 golden（时段/取整/加班/奖励/缺卡）；借款抵扣联动测试

## 非目标

不做社保申报/个税（现状无）。

## Comments
- 2026-07-28 集成：主仓 cherry-pick HR 全量；路由挂到 /hr/*；与打印 catalog stub 注册顺序协调（业务域先于 printing）。
- 2026-07-28 补齐：`verify-hr-operations-rest` 创建 `hr.employee` 编号前先关掉占用的启用规则（bool filter）；编号服务 23505 映射 conflict（对齐 Go）；`:18091` → `ok meta=14 permissionFirst=19 internal=8 imports=31 attendance=17 corrections=8 payroll=24 payments=12 loans=9 audits=17 concurrency=5`；rules+hr.integration 绿。
- 2026-07-28 收口复验：`:18091` hr-operations 全绿；rules golden + 借款联动集成绿；无剩余。
- 2026-07-28 主工作区集成（grok-4.5 缺口）：cherry-pick 去重 `cf7b2d2`（公司默认过账科目 PG 集成）/`b0ba293`（04–07 编号 23505→conflict + inventory 自愈 + verify-inventory 停车编号）/`3f84ab7`（09–14 编号 conflict 测 + OCR 默认存储 + HR 编号腾空 + market fixture）/`bc43cef`（todo 忽略复位）/`4358af8`（printing render 冒烟）/`b8538aa`（setup 空库 e2e afterAll 超时）；合并重复 numberingWriteError；app/index/Meta/helpers 已完整装配，未改 server-go。

- 2026-07-28 独立全量验收（主工作区 grok-4.5）：`server typecheck` 绿；`SYNIE_TEST_DATABASE_URL=…synie_test bun test` 223 pass；`web typecheck` + `bun test` 92 pass；shared decimal 5 pass。活 API :18095 对独立库：17 个 `verify-*.ts` 全绿 + setup 空库 e2e（synie_setup_e2e）+ `verify-web-hc-api` 关键路径绿。修 verify 空库/setup 自愈（inventory 公司单位、printing 默认存储、accounting/quotation 编号规则临时停用）。未执行工单 18；未 push/reset；未改 server-go。
