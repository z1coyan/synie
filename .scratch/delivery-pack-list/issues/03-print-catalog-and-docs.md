# 03 — 打印循环区接入与文档同步

**What to build:** 装箱清单作为发货单 has_many（`pack_lines`）接入 meta，核对打印字段目录自动派生 `${pack_lines.*}` 循环区（含 belongs_to 一层路径、排除 `*_id`，规则同既有循环区）；嵌套循环限制不受影响（单层关系）。同步产品文档：`docs/产品文档/销售发货.md` 新增装箱清单一节（概述边界：纯实物复核层不落账；功能清单：发货录入补子表、审核段补全有或全无校验、打印段补循环区）；核对根 `CONTEXT.md`「装箱清单（行）」「装箱单」词条与实现一致（词条已按定案口径预先写入）；登记 `docs/产品文档/README.md` 若有模块表变更。

**Blocked by:** 01 — 装箱行端到端骨架（02 可并行，不互阻）

**Status:** resolved

- [x] 发货单 meta 增加 `pack_lines` has_many，打印字段目录自动出现 `${pack_lines.*}` 循环区
- [x] 用一份含装箱循环区的测试模板验证渲染（0 行时整行删除的行為同既有循环区）
- [x] `docs/产品文档/销售发货.md` 新增装箱清单一节并登记 README
- [x] 核对 CONTEXT.md 装箱词条与实现一致（如有出入改文档对齐实现）
