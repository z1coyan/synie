# 04 — 生产入库 + 超入设置 + 库存与完工回写

**What to build:** 仓管对生产工单做生产入库（草稿 → 审核 → 可作废）：行必挂工单、物料一致、可分次；审核写正向库存分录、累加已入、削减未完成；`mfg_setting` 生产入库超入比例（默认 0，0～100%），审核时累计已入 ≤ 工单数量 × (1+比例)；满量或容差内超入后工单完工并回写需求行已完成；作废回滚分录与数量。权限 `mfg.output`。对齐采购入库分次/容差模型，不过生产成本总账。

**Blocked by:** 03 — 生产工单（一需求行一未作废工单）

**Status:** resolved

**Parent:** [.scratch/fulfillment-demand-production/spec.md](../spec.md)

- [ ] `mfg_setting` 单行：生产入库超入比例，默认 0，范围 0～1；权限 `mfg.setting`；界面可改
- [ ] 生产入库状态机草稿/已审核/已作废；仅草稿可改可删；行挂工单、仓为启用叶子、base 折算
- [ ] 审核：正向库存分录（voucher 指向生产入库）、累加工单已入；草稿不硬卡超入、审核硬拦
- [ ] 超入比例 0 禁超入；比例 >0 时容差内成功、超上限失败
- [ ] 累计已入 ≥ 工单数量 → 工单已完工 + 需求行已完成
- [ ] 作废：分录作废（过负库存校验）+ 回滚已入；未完成回升则工单回进行中、行回已安排（仍有工单时）
- [ ] 有已审核未作废入库时工单不可作废（与 03 规则闭合）
- [ ] 菜单「生产 → 生产入库」、权限标签、gridMeta、authz 世界、编号
- [ ] Ash 集成测试对标采购入库：分次、分录、超入、作废回滚、完工回写

## Answer

已在 `4c58858` 落地后端主链与基础前端；详见 commit。
