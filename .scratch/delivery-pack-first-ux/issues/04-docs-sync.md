# 04 — 文档同步：产品文档与术语核对

**What to build:** 按仓库文档约定（AGENTS.md：交付新功能必须同步产品文档）更新 `docs/产品文档/销售发货.md`——

- 界面结构段：抽屉两 tab（发货信息＝头＋条目＋借贷科目；装箱清单＝装箱行独占＋行数徽标）。
- 装箱清单一节：物料候选口径从「本单发货条目带出筛选」改为「可发货订单条目池去重物料」（与订单条目弹窗同口径）；可加行条件放宽为选齐公司与对手。
- 新增「从装箱清单获取」小节：按钮位置与禁用/拦截条件；增量补齐语义（已有条目不动物料）；FIFO 分摊规则（订单日期升序、装满再扣、可拆行）；尾差只分不超（报用户人工处理）；币种锁定（现有行优先、否则最早候选定币）；生成行数量精确折算保证审核装箱等式无损；结果反馈两档（sonner/弹框三组）。
- 核对根 `CONTEXT.md`「装箱清单（行）」「发货条目」词条与实现一致——本期领域语义未变，**预计无需改词条**；若实现中发现有出入，改文档对齐实现并在本票记录。
- 核对 `docs/产品文档/README.md` 模块表「销售发货」一行摘要是否需要补「装箱清单 tab 与从装箱清单生成」。

**Blocked by:** 03 — 「从装箱清单获取」（文档反映最终行为）

**Status:** resolved

- [x] `docs/产品文档/销售发货.md` 更新：tab 结构、物料候选口径、按钮全规则
- [x] `docs/产品文档/README.md` 模块表摘要核对/更新
- [x] CONTEXT.md 词条核对(无变更——领域语义未动,装箱词条不含 UI 候选口径)（预计无变更；有出入则改文档对齐实现并记录）
