# 05 — 箱号自动递增：新增行默认尾数 +1

**What to build:** 装箱常态定案为**一物料拆多箱为主**（ grilling 追加，2026-07-29，用户拍板 B），新增装箱行的箱号默认值从「沿用上一行」改为「上一行箱号尾数 +1」：保留前缀与补零宽度（`A-01`→`A-02`、`箱3`→`箱4`、`1`→`2`），位数自然增长不截断（`A-99`→`A-100`），尾无数字追加 `-01`（`托盘甲`→`托盘甲-01`），首行无参照时留空手填；要同箱（一箱多物料）手改回来即可。递增逻辑为纯函数 `web/app/lib/box-no.ts`（BigInt 尾数运算，大编号不丢精度）配 `bun test` 单测；领域语义不变（箱号仍必填自由文本、不走编号规则体系）。同步 `docs/产品文档/销售发货.md` 装箱清单一节。

**Blocked by:** 无 — 可立即开工

**Status:** resolved

- [x] `nextBoxNo` 纯函数＋单测（补零保留/位数增长/尾无数字/空串/大数精度，4 例全绿）
- [x] 装箱行表单箱号 `defaultValue` 改为 `nextBoxNo(上一行箱号)`；首行留空
- [x] `docs/产品文档/销售发货.md` 同步（沿用 → 尾数递增）
- [x] 手工验证（Playwright：A-01 行保存后新增行默认 A-02）
