# 02 — 多循环区渲染

**What to build:** 同一模板允许挂多个顺序循环区（如 BOM 的配料行/副产品行/工艺路线行各占一段），按关系名寻址，逐区行复制展开、逐区 `_seq` 编号、某区 0 行时仅删该区模板行；校验放开多循环区，嵌套循环仍拒并点名。

**Blocked by:** 01 — 路径式占位符端到端打通

**Status:** resolved

- [ ] 多循环区模板（两个及以上不同 has_many）上传校验通过
- [ ] 导出时各循环区按各自数据行数复制展开，`_seq` 逐区从 1 编号
- [ ] 某循环区 0 行时该区模板行整行删除，其余循环区与头字段不受影响
- [ ] 循环区下方内容与 mergeCell 顺移正确（沿 v1 语义逐区适用）
- [ ] 嵌套循环（循环区目标资源的 has_many）仍被拒并点名
- [ ] 集成测试：多循环区资源模板上传 → 导出断言各区行数与序号
