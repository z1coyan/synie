# 03 — 全资源开放 + 模板管理页动态化

**What to build:** 任意权限目录资源都能上传打印模板：field-catalog API 对任意资源返回派生清单（`{fields, loops: [{name, fields}]}`）；模板管理页资源下拉从硬编码两项改为权限目录全量（中文名分域展示）；字段清单面板按「头字段 / 各循环区」分组，标签=路径名。无 has_many 资源（物料、工资单等）退化为纯字段替换。打印/导出入口启用机制不变。

**Blocked by:** 01 — 路径式占位符端到端打通（与 02 可并行）

**Status:** resolved

- [ ] field-catalog API 对任意权限目录资源返回 `{fields, loops:[{name, fields}]}`，未知资源报错照旧
- [ ] 模板管理页资源下拉动态化（权限目录资源、中文名分域），硬编码两项删除
- [ ] 字段清单面板按头字段/各循环区分组显示，条目直接显示路径名
- [ ] 无 has_many 资源模板上传 → 导出纯字段替换成功（集成测试，如物料）
- [ ] 打印/导出/批量打印入口仍随标准动作逐资源启用，未被本票批量打开
- [ ] 前端相关测试/类型检查通过
