# 05 — 树形资源移动端常驻平铺

**What to build:** 树形 grid（物料分类等配了 tree 的列表）在卡片模式下不做逐层展开：`treeActive` 恒为 false，直接以平铺卡片列出节点，追加翻页走 02 的加载更多；找节点靠顶部搜索（沿用现有「筛选即平铺」回退路径，只是移动端让它常驻）；树形页面本就不显示的排序入口维持隐藏。桌面树形展开行为零变化。

**Blocked by:** 02 — 加载更多翻页

**Status:** ready-for-agent

- [x] 树形资源在卡片模式下呈现为平铺卡片列表，无展开箭头、无懒加载占位行
- [x] 平铺列表按加载更多追加翻页，总数口径与桌面平铺回退时一致
- [x] 搜索/筛选在树形卡片模式照常生效（与桌面「筛选即平铺」同口径）
- [x] 排序入口不出现（同树形桌面形态）
- [x] 桌面树形的逐层懒加载展开、筛选回退、清空恢复行为零回归

## Comments

- 实现:cardMode 不再排除 tree;卡片模式下 treeActive 恒 false,复用既有「筛选即平铺」路径,追加翻页走加载更多;冒烟验证物料分类平铺、无 treegrid、无排序入口。
