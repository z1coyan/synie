defmodule SynieCore.Repo.Migrations.AddAccBillTransaction do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:acc_bill_transaction, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :doc_no, :text
      add :transaction_type, :text, null: false
      add :occurred_on, :date, null: false
      add :sub_start, :bigint, null: false
      add :sub_end, :bigint, null: false
      add :amount, :decimal, null: false
      add :party_type, :text
      add :party_id, :uuid
      add :discount_org, :text
      add :discount_rate, :decimal
      add :interest, :decimal
      add :net_amount, :decimal
      add :posting_date, :date
      add :status, :text, null: false, default: "draft"
      add :audited_at, :utc_datetime_usec
      add :remarks, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :company_id,
          references(:bas_company,
            column: :id,
            name: "acc_bill_transaction_company_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false

      add :bank_account_id,
          references(:acc_bank_account,
            column: :id,
            name: "acc_bill_transaction_bank_account_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false

      add :to_bank_account_id,
          references(:acc_bank_account,
            column: :id,
            name: "acc_bill_transaction_to_bank_account_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :bill_id,
          references(:acc_bill,
            column: :id,
            name: "acc_bill_transaction_bill_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :bill_account_id,
          references(:bas_account,
            column: :id,
            name: "acc_bill_transaction_bill_account_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :settle_account_id,
          references(:bas_account,
            column: :id,
            name: "acc_bill_transaction_settle_account_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :interest_account_id,
          references(:bas_account,
            column: :id,
            name: "acc_bill_transaction_interest_account_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :created_by_id,
          references(:sys_user,
            column: :id,
            name: "acc_bill_transaction_created_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :audited_by_id,
          references(:sys_user,
            column: :id,
            name: "acc_bill_transaction_audited_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end

    create index(:acc_bill_transaction, [:company_id, :bank_account_id, :occurred_on])

    create index(:acc_bill_transaction, [:company_id, :status])

    create index(:acc_bill_transaction, [:bill_id, :status])

    create index(:acc_bill_transaction, [:company_id, :doc_no],
             name: "acc_bill_transaction_doc_no_uniq",
             unique: true,
             where: "doc_no IS NOT NULL"
           )
  end

  def down do
    drop constraint(:acc_bill_transaction, "acc_bill_transaction_company_id_fkey")

    drop constraint(:acc_bill_transaction, "acc_bill_transaction_bank_account_id_fkey")

    drop constraint(:acc_bill_transaction, "acc_bill_transaction_to_bank_account_id_fkey")

    drop constraint(:acc_bill_transaction, "acc_bill_transaction_bill_id_fkey")

    drop constraint(:acc_bill_transaction, "acc_bill_transaction_bill_account_id_fkey")

    drop constraint(:acc_bill_transaction, "acc_bill_transaction_settle_account_id_fkey")

    drop constraint(:acc_bill_transaction, "acc_bill_transaction_interest_account_id_fkey")

    drop constraint(:acc_bill_transaction, "acc_bill_transaction_created_by_id_fkey")

    drop constraint(:acc_bill_transaction, "acc_bill_transaction_audited_by_id_fkey")

    drop_if_exists index(:acc_bill_transaction, [:company_id, :doc_no],
                     name: "acc_bill_transaction_doc_no_uniq"
                   )

    drop_if_exists index(:acc_bill_transaction, [:bill_id, :status])

    drop_if_exists index(:acc_bill_transaction, [:company_id, :status])

    drop_if_exists index(:acc_bill_transaction, [:company_id, :bank_account_id, :occurred_on])

    drop table(:acc_bill_transaction)
  end
end
