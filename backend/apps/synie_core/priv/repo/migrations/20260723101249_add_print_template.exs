defmodule SynieCore.Repo.Migrations.AddPrintTemplate do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:sys_print_template, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text, null: false
      add :resource, :text, null: false
      add :is_default, :boolean, null: false, default: false
      add :remarks, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :file_id,
          references(:sys_file,
            column: :id,
            name: "sys_print_template_file_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false
    end

    create index(:sys_print_template, [:is_default, :resource],
             name: "sys_print_template_one_default_per_resource_index",
             unique: true,
             where: "is_default"
           )
  end

  def down do
    drop constraint(:sys_print_template, "sys_print_template_file_id_fkey")

    drop_if_exists index(:sys_print_template, [:is_default, :resource],
                     name: "sys_print_template_one_default_per_resource_index"
                   )

    drop table(:sys_print_template)
  end
end
