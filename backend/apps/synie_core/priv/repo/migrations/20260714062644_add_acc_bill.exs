defmodule SynieCore.Repo.Migrations.AddAccBill do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:acc_bill, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :bill_no, :text, null: false
      add :bill_kind, :text, null: false
      add :issue_date, :date
      add :due_date, :date, null: false
      add :face_amount, :decimal, null: false
      add :drawer_name, :text
      add :drawer_account, :text
      add :drawer_bank_name, :text
      add :drawer_bank_no, :text
      add :payee_name, :text
      add :payee_account, :text
      add :payee_bank_name, :text
      add :payee_bank_no, :text
      add :acceptor_name, :text
      add :acceptor_account, :text
      add :acceptor_bank_name, :text
      add :acceptor_bank_no, :text
      add :transferable, :boolean, null: false, default: true
      add :acceptance_date, :date
      add :remarks, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create index(:acc_bill, [:due_date])

    create unique_index(:acc_bill, [:bill_no], name: "acc_bill_unique_bill_no_index")
  end

  def down do
    drop_if_exists unique_index(:acc_bill, [:bill_no], name: "acc_bill_unique_bill_no_index")

    drop_if_exists index(:acc_bill, [:due_date])

    drop table(:acc_bill)
  end
end
