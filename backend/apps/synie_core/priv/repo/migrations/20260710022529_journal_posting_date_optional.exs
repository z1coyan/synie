defmodule SynieCore.Repo.Migrations.JournalPostingDateOptional do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:acc_gl_journal) do
      modify :posting_date, :date, null: true
    end
  end

  def down do
    alter table(:acc_gl_journal) do
      modify :posting_date, :date, null: false
    end
  end
end
