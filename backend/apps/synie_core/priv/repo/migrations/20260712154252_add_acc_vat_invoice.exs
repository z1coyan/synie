defmodule SynieCore.Repo.Migrations.AddAccVatInvoice do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:acc_vat_invoice, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :doc_no, :text
      add :direction, :text, null: false
      add :invoice_date, :date
      add :posting_date, :date
      add :party_type, :text, null: false
      add :party_id, :uuid, null: false
      add :invoice_kind, :text, null: false
      add :invoice_code, :text, null: false, default: ""
      add :invoice_no, :text
      add :seller_name, :text
      add :seller_tax_no, :text
      add :seller_address_phone, :text
      add :seller_bank_account, :text
      add :buyer_name, :text
      add :buyer_tax_no, :text
      add :buyer_address_phone, :text
      add :buyer_bank_account, :text
      add :items, {:array, :map}, null: false, default: []
      add :net_total, :decimal
      add :tax_total, :decimal
      add :gross_total, :decimal
      add :issuer, :text
      add :reviewer, :text
      add :payee, :text
      add :remarks, :text
      add :red_invoice_no, :text
      add :status, :text, null: false, default: "draft"
      add :audited_at, :utc_datetime_usec

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :company_id,
          references(:bas_company,
            column: :id,
            name: "acc_vat_invoice_company_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false

      add :party_account_id,
          references(:bas_account,
            column: :id,
            name: "acc_vat_invoice_party_account_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :amount_account_id,
          references(:bas_account,
            column: :id,
            name: "acc_vat_invoice_amount_account_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :tax_account_id,
          references(:bas_account,
            column: :id,
            name: "acc_vat_invoice_tax_account_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :mirror_invoice_id,
          references(:acc_vat_invoice,
            column: :id,
            name: "acc_vat_invoice_mirror_invoice_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nilify_all
          )

      add :created_by_id,
          references(:sys_user,
            column: :id,
            name: "acc_vat_invoice_created_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :audited_by_id,
          references(:sys_user,
            column: :id,
            name: "acc_vat_invoice_audited_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end

    create index(:acc_vat_invoice, [:company_id, :invoice_date])

    create index(:acc_vat_invoice, [:company_id, :status])

    create index(:acc_vat_invoice, [:company_id, :doc_no],
             name: "acc_vat_invoice_doc_no_uniq",
             unique: true,
             where: "doc_no IS NOT NULL"
           )

    create index(:acc_vat_invoice, [:company_id, :invoice_code, :invoice_no],
             name: "acc_vat_invoice_no_uniq",
             unique: true,
             where: "invoice_no IS NOT NULL"
           )
  end

  def down do
    drop constraint(:acc_vat_invoice, "acc_vat_invoice_company_id_fkey")

    drop constraint(:acc_vat_invoice, "acc_vat_invoice_party_account_id_fkey")

    drop constraint(:acc_vat_invoice, "acc_vat_invoice_amount_account_id_fkey")

    drop constraint(:acc_vat_invoice, "acc_vat_invoice_tax_account_id_fkey")

    drop constraint(:acc_vat_invoice, "acc_vat_invoice_mirror_invoice_id_fkey")

    drop constraint(:acc_vat_invoice, "acc_vat_invoice_created_by_id_fkey")

    drop constraint(:acc_vat_invoice, "acc_vat_invoice_audited_by_id_fkey")

    drop_if_exists index(:acc_vat_invoice, [:company_id, :invoice_code, :invoice_no],
                     name: "acc_vat_invoice_no_uniq"
                   )

    drop_if_exists index(:acc_vat_invoice, [:company_id, :doc_no],
                     name: "acc_vat_invoice_doc_no_uniq"
                   )

    drop_if_exists index(:acc_vat_invoice, [:company_id, :status])

    drop_if_exists index(:acc_vat_invoice, [:company_id, :invoice_date])

    drop table(:acc_vat_invoice)
  end
end
