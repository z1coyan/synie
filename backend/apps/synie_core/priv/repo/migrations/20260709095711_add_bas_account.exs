defmodule SynieCore.Repo.Migrations.AddBasAccount do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:bas_account, primary_key: false) do
      add(:id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true)
      add(:code, :text, null: false)
      add(:name, :text, null: false)
      add(:direction, :text, null: false)
      add(:is_group, :boolean, null: false, default: false)
      add(:active, :boolean, null: false, default: true)

      add(:inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )

      add(:updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )

      add(
        :parent_id,
        references(:bas_account,
          column: :id,
          name: "bas_account_parent_id_fkey",
          type: :uuid,
          prefix: "public"
        )
      )

      add(
        :company_id,
        references(:bas_company,
          column: :id,
          name: "bas_account_company_id_fkey",
          type: :uuid,
          prefix: "public"
        ), null: false)

      add(
        :currency_id,
        references(:bas_currency,
          column: :id,
          name: "bas_account_currency_id_fkey",
          type: :uuid,
          prefix: "public"
        )
      )
    end

    create(
      unique_index(:bas_account, [:company_id, :code],
        name: "bas_account_unique_code_per_company_index"
      )
    )
  end

  def down do
    drop(constraint(:bas_account, "bas_account_company_id_fkey"))

    drop(constraint(:bas_account, "bas_account_currency_id_fkey"))

    drop_if_exists(
      unique_index(:bas_account, [:company_id, :code],
        name: "bas_account_unique_code_per_company_index"
      )
    )

    drop(constraint(:bas_account, "bas_account_parent_id_fkey"))

    drop(table(:bas_account))
  end
end
