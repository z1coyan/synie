defmodule SynieCore.Repo.Migrations.AddAccountRole do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:bas_account) do
      add :role, :text
    end
  end

  def down do
    alter table(:bas_account) do
      remove :role
    end
  end
end
