defmodule SynieCore.Repo.Migrations.AddInvWarehouse do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:inv_warehouse, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text, null: false
      add :is_leaf, :boolean, null: false, default: true
      add :active, :boolean, null: false, default: true
      add :is_outsourced, :boolean, null: false, default: false
      add :allow_negative, :boolean, null: false, default: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :company_id,
          references(:bas_company,
            column: :id,
            name: "inv_warehouse_company_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :parent_id,
          references(:inv_warehouse,
            column: :id,
            name: "inv_warehouse_parent_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :account_id,
          references(:bas_account,
            column: :id,
            name: "inv_warehouse_account_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end

    create unique_index(:inv_warehouse, [:company_id, :name],
             name: "inv_warehouse_unique_name_per_company_index"
           )
  end

  def down do
    drop constraint(:inv_warehouse, "inv_warehouse_company_id_fkey")

    drop constraint(:inv_warehouse, "inv_warehouse_parent_id_fkey")

    drop constraint(:inv_warehouse, "inv_warehouse_account_id_fkey")

    drop_if_exists unique_index(:inv_warehouse, [:company_id, :name],
                     name: "inv_warehouse_unique_name_per_company_index"
                   )

    drop table(:inv_warehouse)
  end
end
