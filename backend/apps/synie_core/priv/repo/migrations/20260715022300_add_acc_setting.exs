defmodule SynieCore.Repo.Migrations.AddAccSetting do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:acc_setting, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :ocr_access_key_id, :text
      add :ocr_access_key_secret, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    # 单行 seed:资源不开放 create,行在此一次性创建
    execute(
      "INSERT INTO acc_setting (id, inserted_at, updated_at) VALUES (gen_random_uuid(), now(), now())",
      "DELETE FROM acc_setting"
    )
  end

  def down do
    drop table(:acc_setting)
  end
end
