defmodule SynieCore.Repo.Migrations.RenameSysCurrencyToBasCurrency do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    rename(table(:sys_currency), to: table(:bas_currency))

    execute(
      "ALTER INDEX sys_currency_unique_iso_code_index RENAME TO bas_currency_unique_iso_code_index"
    )
  end

  def down do
    execute(
      "ALTER INDEX bas_currency_unique_iso_code_index RENAME TO sys_currency_unique_iso_code_index"
    )

    rename(table(:bas_currency), to: table(:sys_currency))
  end
end
