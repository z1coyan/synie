defmodule SynieCore.Repo.Migrations.AddSysStorage do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:sys_storage, primary_key: false) do
      add(:id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true)
      add(:name, :text, null: false)
      add(:label, :text, null: false)
      add(:kind, :text, null: false)
      add(:root, :text)
      add(:endpoint, :text)
      add(:region, :text)
      add(:bucket, :text)
      add(:prefix, :text)
      add(:access_key_id, :text)
      add(:secret_access_key, :text)
      add(:builtin, :boolean, null: false, default: false)
      add(:is_default, :boolean, null: false, default: false)

      add(:inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )

      add(:updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )
    end

    create(
      index(:sys_storage, [:is_default],
        name: "sys_storage_single_default_index",
        unique: true,
        where: "is_default"
      )
    )

    create(unique_index(:sys_storage, [:name], name: "sys_storage_unique_name_index"))
  end

  def down do
    drop_if_exists(unique_index(:sys_storage, [:name], name: "sys_storage_unique_name_index"))

    drop_if_exists(index(:sys_storage, [:is_default], name: "sys_storage_single_default_index"))

    drop(table(:sys_storage))
  end
end
