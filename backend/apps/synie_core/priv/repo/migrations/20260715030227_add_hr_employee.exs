defmodule SynieCore.Repo.Migrations.AddHrEmployee do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:hr_employees, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :code, :text, null: false
      add :name, :text, null: false
      add :attendance_no, :text
      add :id_number, :text
      add :household_registration, :text
      add :phone, :text
      add :current_address, :text
      add :daily_wage, :decimal
      add :monthly_allowance, :decimal

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:hr_employees, [:code], name: "hr_employees_unique_code_index")

    create unique_index(:hr_employees, [:id_number], name: "hr_employees_unique_id_number_index")
  end

  def down do
    drop_if_exists unique_index(:hr_employees, [:id_number],
                     name: "hr_employees_unique_id_number_index"
                   )

    drop_if_exists unique_index(:hr_employees, [:code], name: "hr_employees_unique_code_index")

    drop table(:hr_employees)
  end
end
