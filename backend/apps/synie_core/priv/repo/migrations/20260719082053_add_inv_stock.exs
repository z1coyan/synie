defmodule SynieCore.Repo.Migrations.AddInvStock do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:inv_stock_doc, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :doc_no, :text, null: false
      add :direction, :text, null: false
      add :doc_date, :date, null: false, default: fragment("CURRENT_DATE")
      add :summary, :text
      add :remarks, :text
      add :status, :text, null: false, default: "draft"
      add :audited_at, :utc_datetime_usec

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :company_id,
          references(:bas_company,
            column: :id,
            name: "inv_stock_doc_company_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :warehouse_id,
          references(:inv_warehouse,
            column: :id,
            name: "inv_stock_doc_warehouse_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :created_by_id,
          references(:sys_user,
            column: :id,
            name: "inv_stock_doc_created_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :audited_by_id,
          references(:sys_user,
            column: :id,
            name: "inv_stock_doc_audited_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end

    create unique_index(:inv_stock_doc, [:doc_no], name: "inv_stock_doc_unique_doc_no_index")

    create table(:inv_stock_transfer_item, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :idx, :bigint, null: false
      add :qty, :decimal, null: false
      add :base_qty, :decimal, null: false, default: "0"
      add :received_qty, :decimal
      add :material_code, :text, null: false
      add :material_name, :text, null: false
      add :material_spec, :text
      add :unit_name, :text, null: false
      add :remark, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :stock_transfer_id, :uuid, null: false
      add :company_id, :uuid, null: false
      add :material_id, :uuid, null: false
      add :unit_id, :uuid, null: false
    end

    create table(:inv_stock_doc_item, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :idx, :bigint, null: false
      add :qty, :decimal, null: false
      add :base_qty, :decimal, null: false, default: "0"
      add :material_code, :text, null: false
      add :material_name, :text, null: false
      add :material_spec, :text
      add :unit_name, :text, null: false
      add :remark, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :stock_doc_id,
          references(:inv_stock_doc,
            column: :id,
            name: "inv_stock_doc_item_stock_doc_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :delete_all
          ), null: false

      add :company_id,
          references(:bas_company,
            column: :id,
            name: "inv_stock_doc_item_company_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :material_id,
          references(:inv_material,
            column: :id,
            name: "inv_stock_doc_item_material_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :unit_id,
          references(:bas_unit,
            column: :id,
            name: "inv_stock_doc_item_unit_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false
    end

    create table(:inv_stock_transfer, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
    end

    alter table(:inv_stock_transfer_item) do
      modify :stock_transfer_id,
             references(:inv_stock_transfer,
               column: :id,
               name: "inv_stock_transfer_item_stock_transfer_id_fkey",
               type: :uuid,
               prefix: "public",
               on_delete: :delete_all
             )

      modify :company_id,
             references(:bas_company,
               column: :id,
               name: "inv_stock_transfer_item_company_id_fkey",
               type: :uuid,
               prefix: "public"
             )

      modify :material_id,
             references(:inv_material,
               column: :id,
               name: "inv_stock_transfer_item_material_id_fkey",
               type: :uuid,
               prefix: "public"
             )

      modify :unit_id,
             references(:bas_unit,
               column: :id,
               name: "inv_stock_transfer_item_unit_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end

    alter table(:inv_stock_transfer) do
      add :doc_no, :text, null: false
      add :doc_date, :date, null: false, default: fragment("CURRENT_DATE")
      add :summary, :text
      add :remarks, :text
      add :status, :text, null: false, default: "draft"
      add :shipped_at, :utc_datetime_usec
      add :received_at, :utc_datetime_usec

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :company_id,
          references(:bas_company,
            column: :id,
            name: "inv_stock_transfer_company_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :from_warehouse_id,
          references(:inv_warehouse,
            column: :id,
            name: "inv_stock_transfer_from_warehouse_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :to_warehouse_id,
          references(:inv_warehouse,
            column: :id,
            name: "inv_stock_transfer_to_warehouse_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :transit_warehouse_id,
          references(:inv_warehouse,
            column: :id,
            name: "inv_stock_transfer_transit_warehouse_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :created_by_id,
          references(:sys_user,
            column: :id,
            name: "inv_stock_transfer_created_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :shipped_by_id,
          references(:sys_user,
            column: :id,
            name: "inv_stock_transfer_shipped_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :received_by_id,
          references(:sys_user,
            column: :id,
            name: "inv_stock_transfer_received_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end

    create unique_index(:inv_stock_transfer, [:doc_no],
             name: "inv_stock_transfer_unique_doc_no_index"
           )

    create table(:inv_stock_entry, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :seq, :bigserial, null: false
      add :quantity, :decimal, null: false
      add :posting_date, :date, null: false
      add :voucher_type, :text, null: false
      add :voucher_id, :uuid, null: false
      add :voucher_no, :text, null: false
      add :is_cancelled, :boolean, null: false, default: false
      add :remarks, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :company_id,
          references(:bas_company,
            column: :id,
            name: "inv_stock_entry_company_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :warehouse_id,
          references(:inv_warehouse,
            column: :id,
            name: "inv_stock_entry_warehouse_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :material_id,
          references(:inv_material,
            column: :id,
            name: "inv_stock_entry_material_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false
    end

    create index(:inv_stock_entry, [:voucher_type, :voucher_id])

    create index(:inv_stock_entry, [:company_id, :warehouse_id, :material_id, :posting_date])

    create constraint(:inv_stock_entry, :quantity_nonzero,
             check: """
               quantity <> 0
             """
           )

    create constraint(:inv_stock_doc_item, :qty_positive,
             check: """
               qty > 0
             """
           )

    create constraint(:inv_stock_transfer_item, :qty_positive,
             check: """
               qty > 0
             """
           )
  end

  def down do
    drop_if_exists constraint(:inv_stock_transfer_item, :qty_positive)

    drop_if_exists constraint(:inv_stock_doc_item, :qty_positive)

    drop_if_exists constraint(:inv_stock_entry, :quantity_nonzero)

    drop constraint(:inv_stock_entry, "inv_stock_entry_company_id_fkey")

    drop constraint(:inv_stock_entry, "inv_stock_entry_warehouse_id_fkey")

    drop constraint(:inv_stock_entry, "inv_stock_entry_material_id_fkey")

    drop_if_exists index(:inv_stock_entry, [
                     :company_id,
                     :warehouse_id,
                     :material_id,
                     :posting_date
                   ])

    drop_if_exists index(:inv_stock_entry, [:voucher_type, :voucher_id])

    drop table(:inv_stock_entry)

    drop constraint(:inv_stock_transfer, "inv_stock_transfer_company_id_fkey")

    drop constraint(:inv_stock_transfer, "inv_stock_transfer_from_warehouse_id_fkey")

    drop constraint(:inv_stock_transfer, "inv_stock_transfer_to_warehouse_id_fkey")

    drop constraint(:inv_stock_transfer, "inv_stock_transfer_transit_warehouse_id_fkey")

    drop constraint(:inv_stock_transfer, "inv_stock_transfer_created_by_id_fkey")

    drop constraint(:inv_stock_transfer, "inv_stock_transfer_shipped_by_id_fkey")

    drop constraint(:inv_stock_transfer, "inv_stock_transfer_received_by_id_fkey")

    drop_if_exists unique_index(:inv_stock_transfer, [:doc_no],
                     name: "inv_stock_transfer_unique_doc_no_index"
                   )

    alter table(:inv_stock_transfer) do
      remove :received_by_id
      remove :shipped_by_id
      remove :created_by_id
      remove :transit_warehouse_id
      remove :to_warehouse_id
      remove :from_warehouse_id
      remove :company_id
      remove :updated_at
      remove :inserted_at
      remove :received_at
      remove :shipped_at
      remove :status
      remove :remarks
      remove :summary
      remove :doc_date
      remove :doc_no
    end

    drop constraint(:inv_stock_transfer_item, "inv_stock_transfer_item_stock_transfer_id_fkey")

    drop constraint(:inv_stock_transfer_item, "inv_stock_transfer_item_company_id_fkey")

    drop constraint(:inv_stock_transfer_item, "inv_stock_transfer_item_material_id_fkey")

    drop constraint(:inv_stock_transfer_item, "inv_stock_transfer_item_unit_id_fkey")

    alter table(:inv_stock_transfer_item) do
      modify :unit_id, :uuid
      modify :material_id, :uuid
      modify :company_id, :uuid
      modify :stock_transfer_id, :uuid
    end

    drop table(:inv_stock_transfer)

    drop constraint(:inv_stock_doc_item, "inv_stock_doc_item_stock_doc_id_fkey")

    drop constraint(:inv_stock_doc_item, "inv_stock_doc_item_company_id_fkey")

    drop constraint(:inv_stock_doc_item, "inv_stock_doc_item_material_id_fkey")

    drop constraint(:inv_stock_doc_item, "inv_stock_doc_item_unit_id_fkey")

    drop table(:inv_stock_doc_item)

    drop table(:inv_stock_transfer_item)

    drop constraint(:inv_stock_doc, "inv_stock_doc_company_id_fkey")

    drop constraint(:inv_stock_doc, "inv_stock_doc_warehouse_id_fkey")

    drop constraint(:inv_stock_doc, "inv_stock_doc_created_by_id_fkey")

    drop constraint(:inv_stock_doc, "inv_stock_doc_audited_by_id_fkey")

    drop_if_exists unique_index(:inv_stock_doc, [:doc_no],
                     name: "inv_stock_doc_unique_doc_no_index"
                   )

    drop table(:inv_stock_doc)
  end
end
