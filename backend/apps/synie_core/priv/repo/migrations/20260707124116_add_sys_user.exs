defmodule SynieCore.Repo.Migrations.AddSysUser do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:sys_user, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :username, :citext, null: false
      add :name, :text
      add :hashed_password, :text, null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:sys_user, [:username], name: "sys_user_unique_username_index")
  end

  def down do
    drop_if_exists unique_index(:sys_user, [:username], name: "sys_user_unique_username_index")

    drop table(:sys_user)
  end
end
