defmodule SynieCore.Repo.Migrations.AddExpenseReport do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:acc_expense_report, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :doc_no, :text, null: false
      add :expense_date, :date, null: false
      add :posting_date, :date
      add :remarks, :text
      add :status, :text, null: false, default: "draft"
      add :audited_at, :utc_datetime_usec

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :company_id,
          references(:bas_company,
            column: :id,
            name: "acc_expense_report_company_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :employee_id,
          references(:hr_employees,
            column: :id,
            name: "acc_expense_report_employee_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :payment_account_id,
          references(:bas_account,
            column: :id,
            name: "acc_expense_report_payment_account_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :created_by_id,
          references(:sys_user,
            column: :id,
            name: "acc_expense_report_created_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :audited_by_id,
          references(:sys_user,
            column: :id,
            name: "acc_expense_report_audited_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end

    create index(:acc_expense_report, [:company_id, :status])

    create unique_index(:acc_expense_report, [:company_id, :doc_no],
             name: "acc_expense_report_unique_doc_no_per_company_index"
           )

    create table(:acc_expense_report_item, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :idx, :bigint, null: false
      add :kind, :text, null: false
      add :summary, :text
      add :amount, :decimal
      add :remarks, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :report_id,
          references(:acc_expense_report,
            column: :id,
            name: "acc_expense_report_item_report_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :delete_all
          ), null: false

      add :company_id,
          references(:bas_company,
            column: :id,
            name: "acc_expense_report_item_company_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :invoice_id,
          references(:acc_vat_invoice,
            column: :id,
            name: "acc_expense_report_item_invoice_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nothing
          )

      add :expense_account_id,
          references(:bas_account,
            column: :id,
            name: "acc_expense_report_item_expense_account_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end
  end

  def down do
    drop constraint(:acc_expense_report_item, "acc_expense_report_item_report_id_fkey")

    drop constraint(:acc_expense_report_item, "acc_expense_report_item_company_id_fkey")

    drop constraint(:acc_expense_report_item, "acc_expense_report_item_invoice_id_fkey")

    drop constraint(:acc_expense_report_item, "acc_expense_report_item_expense_account_id_fkey")

    drop table(:acc_expense_report_item)

    drop constraint(:acc_expense_report, "acc_expense_report_company_id_fkey")

    drop constraint(:acc_expense_report, "acc_expense_report_employee_id_fkey")

    drop constraint(:acc_expense_report, "acc_expense_report_payment_account_id_fkey")

    drop constraint(:acc_expense_report, "acc_expense_report_created_by_id_fkey")

    drop constraint(:acc_expense_report, "acc_expense_report_audited_by_id_fkey")

    drop_if_exists unique_index(:acc_expense_report, [:company_id, :doc_no],
                     name: "acc_expense_report_unique_doc_no_per_company_index"
                   )

    drop_if_exists index(:acc_expense_report, [:company_id, :status])

    drop table(:acc_expense_report)
  end
end
