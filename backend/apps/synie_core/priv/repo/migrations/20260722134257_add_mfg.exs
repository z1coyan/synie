defmodule SynieCore.Repo.Migrations.AddMfg do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:mfg_bom_route, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :seq, :bigint, null: false
      add :requirement, :text
      add :is_outsourced, :boolean, null: false, default: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :bom_id, :uuid, null: false
      add :operation_id, :uuid, null: false
    end

    create table(:mfg_bom_byproduct, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :quantity, :decimal, null: false
      add :note, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :bom_id, :uuid, null: false
      add :material_id, :uuid, null: false
      add :unit_id, :uuid, null: false
    end

    create table(:mfg_bom_component, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :quantity, :decimal, null: false
      add :loss_rate, :decimal
      add :note, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :bom_id, :uuid, null: false
      add :material_id, :uuid, null: false
      add :unit_id, :uuid, null: false
    end

    create table(:mfg_bom, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
    end

    alter table(:mfg_bom_route) do
      modify :bom_id,
             references(:mfg_bom,
               column: :id,
               name: "mfg_bom_route_bom_id_fkey",
               type: :uuid,
               prefix: "public",
               on_delete: :delete_all
             )
    end

    alter table(:mfg_bom_byproduct) do
      modify :bom_id,
             references(:mfg_bom,
               column: :id,
               name: "mfg_bom_byproduct_bom_id_fkey",
               type: :uuid,
               prefix: "public",
               on_delete: :delete_all
             )

      modify :material_id,
             references(:inv_material,
               column: :id,
               name: "mfg_bom_byproduct_material_id_fkey",
               type: :uuid,
               prefix: "public"
             )

      modify :unit_id,
             references(:bas_unit,
               column: :id,
               name: "mfg_bom_byproduct_unit_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end

    alter table(:mfg_bom_component) do
      modify :bom_id,
             references(:mfg_bom,
               column: :id,
               name: "mfg_bom_component_bom_id_fkey",
               type: :uuid,
               prefix: "public",
               on_delete: :delete_all
             )

      modify :material_id,
             references(:inv_material,
               column: :id,
               name: "mfg_bom_component_material_id_fkey",
               type: :uuid,
               prefix: "public"
             )

      modify :unit_id,
             references(:bas_unit,
               column: :id,
               name: "mfg_bom_component_unit_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end

    alter table(:mfg_bom) do
      add :note, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :material_id,
          references(:inv_material,
            column: :id,
            name: "mfg_bom_material_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :restrict
          ), null: false
    end

    create unique_index(:mfg_bom, [:material_id], name: "mfg_bom_unique_material_index")

    create table(:mfg_process_template, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :code, :text, null: false
      add :name, :text, null: false
      add :note, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:mfg_process_template, [:code],
             name: "mfg_process_template_unique_code_index"
           )

    create table(:mfg_process_template_item, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :seq, :bigint, null: false
      add :requirement, :text
      add :is_outsourced, :boolean, null: false, default: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :template_id,
          references(:mfg_process_template,
            column: :id,
            name: "mfg_process_template_item_template_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :delete_all
          ), null: false

      add :operation_id, :uuid, null: false
    end

    create table(:mfg_operation, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
    end

    alter table(:mfg_bom_route) do
      modify :operation_id,
             references(:mfg_operation,
               column: :id,
               name: "mfg_bom_route_operation_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end

    alter table(:mfg_process_template_item) do
      modify :operation_id,
             references(:mfg_operation,
               column: :id,
               name: "mfg_process_template_item_operation_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end

    alter table(:mfg_operation) do
      add :code, :text, null: false
      add :name, :text, null: false
      add :note, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:mfg_operation, [:code], name: "mfg_operation_unique_code_index")
  end

  def down do
    drop_if_exists unique_index(:mfg_operation, [:code], name: "mfg_operation_unique_code_index")

    alter table(:mfg_operation) do
      remove :updated_at
      remove :inserted_at
      remove :note
      remove :name
      remove :code
    end

    drop constraint(:mfg_process_template_item, "mfg_process_template_item_operation_id_fkey")

    alter table(:mfg_process_template_item) do
      modify :operation_id, :uuid
    end

    drop constraint(:mfg_bom_route, "mfg_bom_route_operation_id_fkey")

    alter table(:mfg_bom_route) do
      modify :operation_id, :uuid
    end

    drop table(:mfg_operation)

    drop constraint(:mfg_process_template_item, "mfg_process_template_item_template_id_fkey")

    drop table(:mfg_process_template_item)

    drop_if_exists unique_index(:mfg_process_template, [:code],
                     name: "mfg_process_template_unique_code_index"
                   )

    drop table(:mfg_process_template)

    drop constraint(:mfg_bom, "mfg_bom_material_id_fkey")

    drop_if_exists unique_index(:mfg_bom, [:material_id], name: "mfg_bom_unique_material_index")

    alter table(:mfg_bom) do
      remove :material_id
      remove :updated_at
      remove :inserted_at
      remove :note
    end

    drop constraint(:mfg_bom_component, "mfg_bom_component_bom_id_fkey")

    drop constraint(:mfg_bom_component, "mfg_bom_component_material_id_fkey")

    drop constraint(:mfg_bom_component, "mfg_bom_component_unit_id_fkey")

    alter table(:mfg_bom_component) do
      modify :unit_id, :uuid
      modify :material_id, :uuid
      modify :bom_id, :uuid
    end

    drop constraint(:mfg_bom_byproduct, "mfg_bom_byproduct_bom_id_fkey")

    drop constraint(:mfg_bom_byproduct, "mfg_bom_byproduct_material_id_fkey")

    drop constraint(:mfg_bom_byproduct, "mfg_bom_byproduct_unit_id_fkey")

    alter table(:mfg_bom_byproduct) do
      modify :unit_id, :uuid
      modify :material_id, :uuid
      modify :bom_id, :uuid
    end

    drop constraint(:mfg_bom_route, "mfg_bom_route_bom_id_fkey")

    alter table(:mfg_bom_route) do
      modify :bom_id, :uuid
    end

    drop table(:mfg_bom)

    drop table(:mfg_bom_component)

    drop table(:mfg_bom_byproduct)

    drop table(:mfg_bom_route)
  end
end
