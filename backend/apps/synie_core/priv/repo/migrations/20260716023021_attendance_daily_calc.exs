defmodule SynieCore.Repo.Migrations.AttendanceDailyCalc do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:hr_attendance_day, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :date, :date, null: false
      add :morning_in, :time
      add :morning_out, :time
      add :afternoon_in, :time
      add :afternoon_out, :time
      add :normal_hours, :decimal, null: false
      add :overtime_hours, :decimal, null: false
      add :bonus_workday, :decimal, null: false
      add :status, :text, null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :employee_id,
          references(:hr_employees,
            column: :id,
            name: "hr_attendance_day_employee_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false
    end

    create index(:hr_attendance_day, [:date])

    create unique_index(:hr_attendance_day, [:employee_id, :date],
             name: "hr_attendance_day_unique_employee_date_index"
           )

    create table(:hr_attendance_correction, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :date, :date, null: false
      add :times, {:array, :time}, null: false
      add :note, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :employee_id,
          references(:hr_employees,
            column: :id,
            name: "hr_attendance_correction_employee_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :created_by_id,
          references(:sys_user,
            column: :id,
            name: "hr_attendance_correction_created_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end

    create index(:hr_attendance_correction, [:date])

    create unique_index(:hr_attendance_correction, [:employee_id, :date],
             name: "hr_attendance_correction_unique_employee_date_index"
           )
  end

  def down do
    drop constraint(:hr_attendance_correction, "hr_attendance_correction_employee_id_fkey")

    drop constraint(:hr_attendance_correction, "hr_attendance_correction_created_by_id_fkey")

    drop_if_exists unique_index(:hr_attendance_correction, [:employee_id, :date],
                     name: "hr_attendance_correction_unique_employee_date_index"
                   )

    drop_if_exists index(:hr_attendance_correction, [:date])

    drop table(:hr_attendance_correction)

    drop constraint(:hr_attendance_day, "hr_attendance_day_employee_id_fkey")

    drop_if_exists unique_index(:hr_attendance_day, [:employee_id, :date],
                     name: "hr_attendance_day_unique_employee_date_index"
                   )

    drop_if_exists index(:hr_attendance_day, [:date])

    drop table(:hr_attendance_day)
  end
end
