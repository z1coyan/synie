defmodule SynieCore.Repo.Migrations.AddAttachmentCompanyId do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    # 现存附件全部挂全局宿主(sal_customer,无 company_id),company_id 保持 NULL 即正确
    # ——照 sys_audit_log 的 is_nil(company_id) 分支不受公司过滤,无需数据回填。
    alter table(:sys_attachment) do
      add :company_id, :uuid
    end

    create index(:sys_attachment, [:company_id])
  end

  def down do
    drop_if_exists index(:sys_attachment, [:company_id])

    alter table(:sys_attachment) do
      remove :company_id
    end
  end
end
