# 复制为 .env 后按需修改。dev/test 启动时自动加载(见 config/config.exs)。
# 优先级: 进程已有环境变量 > .env.<MIX_ENV> > .env
#
# test 库名见 .env.test(已提交),避免 mix test 误用 synie_dev。

PGHOST=localhost
PGPORT=5440
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=synie_dev
POOL_SIZE=10
