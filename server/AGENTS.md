# server/（Bun + Hono + Kysely）

- 第一语言中文（文档/用户可见文案）；标识符英文。
- 本目录是目标后端；`server-go/` 只是**行为参考**，禁止修改，禁止把 Go 形态机械搬成 class。
- 编码约定、技术栈定案、命令见 `server/README.md`，开工前必读。
- 业务实现顺序与验收以 `.scratch/ts-backend-rewrite/` 的 spec 与工单为准；
  交付或变更业务规则时同步 `docs/产品文档/` 与 `CONTEXT.md`（根 AGENTS.md 约定）。
